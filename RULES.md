# 官方规则与答疑汇总（棒材组合订单高效锯切优化）

**来源**：赛题组在选手群内的公开答疑（发言人 `中文名字` / `JSAI卫` / `SUBARU AWA`），
时间跨度 2026-09-14 ~ 2026-09-21。逐条保留日期与答案原文，便于回溯与复核。

> **本文件属于 `jinyinsai1_nolimit` —— 「无求解时限」fork。**
> 原项目 `jinyinsai1` 冻结为「10 秒单批求解时限」条件下的过程记录；该时限自 09-21 起被
> 官方作废（§0.1），此后以本 fork 为工作版本，故规则以本文为准。

**效力**：这些是**口语答疑**，优先级低于官方 PDF 赛题与 `constraints.txt`；但凡是与赛题组
明确书面口径冲突的，以本文记录为准（赛题组多次自我修正，见 §1.1、§3.2）。

**⚠️ 最重要的结论**：初赛与复赛口径不同，且有 4 处会让现有实现直接出错（§12）；
**另有 09-21 的两项新口径直接推翻既往设定**（§0：求解时间限制作废、刀数基准由 9w 翻到 16w）。

---

## 0. 09-21 的两项新口径（本 fork 的立身之本）

### 0.1 求解时间限制作废

> **赛题文件中与求解时间相关的表述作废，各位选手在复赛和半决赛无须考虑求解时间。
> 如果对自己算法有信心，在决赛汇报材料中，可以对算法的求解效率进行介绍。**（赛题组，09-21）

- 作废对象是赛题 PDF **第五章（三）「单批次订单求解时间应不大于 10 秒」**——选手 09-21 14:51
  正是引用该条款提问"这是个必要条件对嘛"，同日即被宣告作废。
- 因此 `solver.search_10s` 的 `seconds` **不再是合规约束**：预算可以取任意大，也可以把
  「种子 + 退火」的全部时间都投在质量上。**本 fork 按无时限设计**，`solve_semi.py` 的
  `--seconds-per-chunk` 与其 14400 秒总预算都应显著放大后重跑。
- **求解效率不再计分**，只在决赛汇报材料中作为亮点陈述——即"快"不再换分，"好"才换分。
- ⚠️ **两件事不要混为一谈**：
  - 「求解时间」= **算法运行时长**（已作废，明确）；
  - 「时间子项」= **评分表里的那一项权重**（PDF 九为 40/30/20/10），是否随之作废
    **尚未获明确答复**（§13 第 6 条）。两种口径下成材率的实际权重是 0.3 还是 0.4，
    会改变优化重心，故本 fork 需同时保留两种权重的对照实验能力。

### 0.2 复赛基准刀数 = 160000（已实测：**不可能饱和**）

> **复赛基准刀数是 160000**（赛题组，09-21 15:46，答选手 14:51 的"基准锯切刀数是多少"）

- **初赛基准是 90000**（09-18 08:27），复赛**几乎翻倍**。本项目此前所有以 9w 为基准的
  刀数权衡（`baseline_knives`、`_key` 的比较优先级、全部历史校准点）在复赛口径下**作废**。
- **实测（`diagnostics/knives_ideal.py`）**：复赛全量 9,999 单、需求 **10,242,863 根**成品；
  每轮刀数 = `Σk + 1`、每轮产出 = `p × Σk`（`p` = 并排根数，09-15 11:11"刀数不乘棒材根数"），
  故 **刀数/根 = (Σk+1) / (p·Σk)** —— 杠杆是 **`p`（并排根数），不是段数**。
  在宽度 `floor(2000mm / dia)` 与承重 60 t 双上限下逐单取最优形状：

  | | 刀数 | 对 160000 基准 |
  |---|---|---|
  | 理论下界（每轮满载，**不可达**） | **177,736** | 1.11× ⇒ 刀数子项天花板 **36.0 / 40** |
  | 当前实测（1,440 单局部，时限制版本） | 62,750（线性外推 ≈ 436,000） | 2.72× ⇒ 约 **14.7 / 40** |

  两条结论：**①刀数子项永远拿不到满分，上限就是 36 / 40**；
  **②现状离下界还有约 2.5 倍空间**，刀数仍是第一优先级，不能按早先假设的"饱和后转向成材率"来做。
- 最优形状是**短轮次 + 大 `p`**（如 Ø26.5 取 `Σk=22, p=75, len=143 m` ⇒ 0.0139 刀/根），
  与"长轮次摊薄成本"的直觉相反——短轮更轻，`p` 才开得宽。
- ⚠️ 下界不可达：它忽略了共享方案要同时服务多单、每单末轮不满、以及 **`p | pieces` 整除要求**。
  最后一项正是 §0.3 要拆除的约束。
- ⚠️ 同一时刻（09-21 15:47）有选手追问「成材率与刀数优先级一样、权重都是 0.4」，
  **官方未答复**，属选手推测，**不得作为依据**。

#### 0.2.1 ⚠️ 09-22 纠正：177,736 不是下界，36.0/40 也不是天花板

用第二台机器的复赛方案（`runs/theirs_main/`，2,196 方案 / 12,559 轮）在本 fork 自己的
`platform_score` 下复算，拿到 **175,952 刀 —— 低于上面那张表的 177,736**，且
`platform_check` 通过、0 违规、逐直径 cost/piece 全部高于该直径的 tab 值。所以问题在表，不在方案：

- 那张表按**每单单独一轮**记账，把「一组切头切尾（+1）」摊给单个订单。真实方案把同一轮的
  `Σk` 跨多个订单累加，同一个 +1 被更多根成品摊薄，单根成本因此**可以低于**每单独立算出的值。
- 它同时禁止一轮内混不同定尺（`o.size`），而混料能压低长度/件数比、把 `p` 留在床宽上限。
- 故该表只能读作「**每单独立、每轮满载时的参考成本**」。**36.0/40 这个天花板随之作废**：
  实测已达 36.37（对方, floor 口径）/ 33.62（对方, snap）/ 34.47（我方, floor）/ 32.39（我方, snap）。
  「不可能饱和」的定性结论仍成立，但它给的数字偏悲观。
- 复算脚本：`diagnostics/plan_anatomy.py`（逐直径 cost/piece 对照 + 轮/段分解）。

### 0.3 超产约束必须放宽（`max_overproduction_ratio` 现在设成了 0）

- 官方口径：复赛**欠产惩罚**、**不设超产上限**、超产**不扣分**但**不计入成材率分子**
  （09-16 17:01）。`constraints.txt` 第 8 条也只要求"分配总重量**不低于**订单重量"，
  **全文没有任何超产上限条款**。
- 本项目当前 `max_overproduction_ratio = 0` ⇒ `caps[i] = pieces`，即**要求交付精确等于需求**，
  连超产都判违规（`validate_plan` 报 `Demand/overproduction violation`）。
  这**比官方规则严格**，代价很高：它强迫 `p | pieces`（`_solo_scheme.try_shape` 的整除判据），
  把 `p` 锁死在 `pieces` 的因数上——而 §0.2 刚证明 `p` 正是刀数的杠杆。
- 实证代价：`NP01:43` 的 `B20270366`（674 根 = 2 × 337，337 为素数）因此被
  `ModelError: Search found no feasible scheme` 判为无解，而穷举 DP（`diagnostics/solo_feasibility.py`）
  证明它 **2 轮可解**。这不是偶发，是只要 `pieces` 的因数集与可行的 `p` 区间不相交就会触发。
- 应有形态：合法区间 **`delivered ≥ demand`**；成材率分子取 **`min(delivered, demand)`**。

### 0.4 复赛计刀口径：整数倍长度会被浮点 floor 吃掉（**未定**，敞口约 1.4 万刀）

`platform_score.row_metrics` 用 `int(L // size)` 记段。求解器天生把长度写成定尺的整数倍，
而 `L / size` 在二进制浮点下可能落成 `k - 1e-16`，floor 直接吃掉整整一件。实测中招：
我方 11,921 个段、对方 14,389 个段。

| 口径 | 定义 | 我方刀数 | 对方刀数 |
|---|---|---|---|
| floor | `int(L // size)` | 185,648 | 175,952 |
| snap | `int((L + 1e-9·L) // size)` | **197,569** | **190,341** |

- **两套独立实现都按 snap 记账**：我方 `runs/semi_nolimit_v1/summary.json` 记 197,569，
  对方自报 190,341 —— 与 snap 复算**逐位相同**。严格 floor 反而是本 fork 的少数派读法。
- 初赛的官方反馈支持 snap：09-16 的 92.02 反馈显示官方**补回了**被浮点塌掉的段（拆解见
  `entry_knives` 文档串：108,671 = Σk + 2×(k==1 的段)）。若复赛同理，真实分数落在
  **89.72（我方）/ 91.17（对方）**，而不是 91.80 / 93.92。
- ⚠️ 反向风险：既然官方会补，就可以**故意**把长度写成略低于整数倍去吃这份补偿
  （`submission_fill/初赛结果_棒材优化.json` 正是 `X.9999999999` 的路子）。收益 ≈ 2.75 分，
  但属赌口径，未获官方确认。
- 复算脚本：`diagnostics/knife_convention.py`。

### 0.5 成材率有三套数，只有一套合官方规则

我方同一份方案：未封顶 **93.3212%**、按官方口径封顶（超产不计入分子）**91.0793%**、
内部账本自报 **92.0919%**。内部那套的隐含分母 557,341 t 比申报钢坯 549,999.4 t 多 7,342 t（1.3%），
分子取未封顶、分母偏大 —— 两头都不合官方定义（官方：分子剔除超产，分母 = 各轮申报钢坯重量之和）。
⇒ **一律以 `platform_score.evaluate` 的输出为准**，`summary.json` 的 `yield_rate` 不可对外引用。

### 0.6 根因（09-22）：搜索把「同一交付量里最省刀的那张床」丢掉了

问「为什么放开时限反而更差」的答案不是时限，是 `_enumerate_round_shapes.offer()`
（`solver.py`）里一条**方向写反的守卫**。三条同源缺陷，同一个物理事实：

> 一轮的成本是 `Σk + 1` 刀，而它交给订单 j 的量是 `delta_j = k_j · p`。
> 固定 `delta` 时 `k_j = delta_j / p`，故刀数 `= Σdelta / p + 1` 对 `p` **单调递减**，
> 轮长 `= Σ(delta_j·size_j) / p + trim` 同样随 `p` 缩短。
> **同一交付量下，宽床严格更省刀、也更省钢坯，且轮内订单集合不变（不动连续性）。**

| # | 位置 | 缺陷 | 证据 |
|---|---|---|---|
| 1 | `offer()` 守卫 `parallel >= old[0]` | 按 `delta` 去重却保留**先到者**；`parallel` 是 `1→plimit` 升序扫，先到者就是**最窄**的床，更宽的孪生全被否掉 | `diagnostics/shape_dedup_probe.py`：4 个采样分组 13,151 个保留形状中 987 个有更宽孪生，**8,342 刀**可回收；最极端 `delta=(0,341,0)` 保留 `p=11,k=31`（32 刀）而最宽 `p=31,k=11`（12 刀） |
| 2 | 同一处，**预算耗尽方向** | `_SHAPE_WORK_LIMIT=300,000` 耗尽即 `break` 整个 parallel 循环；升序扫描把预算全花在窄床上，宽床**从未被枚举** | `diagnostics/widen_budget_probe.py`：3 单分组（sizes 4.75/5.0/8.15，plimit 46）在 300K 下残留 237 个更宽孪生 / 2,411 刀，把预算提到 1M 后**归零** |
| 3 | `moves.sort(key=(-Σdelta, m[1]))` | 同交付量时把**窄床排前面**，而 `moves[:_MOVE_CAP]`（4000）丢掉的是队尾 ⇒ 截断时先丢宽床 | 与 #1 同源，一并对齐为 `-m[1]` |

- **修法**（`solver.py`，三处）：守卫改 `parallel <= old[0]`（保留最宽）、parallel 扫描改
  **降序** `range(plimit, 0, -1)`（预算先花在宽床上）、tie-break 改 `-m[1]`。
  修复后 `shape_dedup_probe.py`：**4 个分组 12,832 个形状，0 个有更宽孪生，0 刀可回收**。
- **同一条教训在代码库里已有先例**：`_solo_scheme.try_shape`（`solver.py`）本来就是
  `for parallel in range(limit, 0, -1)` 降序，注释写着 "Ascending used to stop at the first
  *legal* shape, which for `B20270366`（674 件, 5.0 m）is `p=24`（147 m 轮, 30 刀）—— 而
  `p=46` 用一轮 77 m、16 刀就装下同一需求"。**只有共享分组的收尾搜索漏了这条**。
- ⚠️ **同一缺陷也存在于 `_construct` 的候选排序**：它按 `useful / r.knives` 选轮，但每个候选的
  `useful` 是用各自的 `spread/k` 算出来的，不是同一交付量下的比较 ⇒ 窄床仍可能在贪心里胜出。
  故 `S S·3` 的源码修复**不能替代**事后整形（见 §0.7）。
- ⚠️ **截断方向换了风险**：升序丢的是宽床，降序丢的是窄床。窄 `p` 的 `delta` 是细粒度整数（`p=1`
  可命中任意余量），`_round_moves` 又只留 `delta ≤ cap` ⇒ 降序可能让收尾搜索少了"精确落点"的
  轮子，退化成超产。**必须用确定性 A/B 判定净效果，见 §0.7。**

### 0.7 事后整形器：把已产出的方案按真实 Δscore 重新定价

`solver.py` 修的是"以后别再丢宽床"；`reshape_plan.py` 修的是"已经丢掉的怎么办"。
它逐轮把床宽顶上去 + 剥掉由此产生的超产，每一步都用**真实边际价格**定价：

```
∂score/∂knife = 40·baseline/K²  = 1.86e-4      （K ≈ 185,648, baseline 160000）
∂score/∂kg    = 30·A/R²         = 4.97e-8      （A/R = 封顶成材率 / 申报钢坯）
⇒ 一根 9.6 t 坯料 ≈ 2.6 刀
```

- 只在 `gain = per_knife·Δknife − per_kg·Δraw > 0` 时才接受改写；`widen` 与 `shed` 交替跑。
- **`shed` 的两条硬约束**：`k ≥ 2`（把订单从中间轮次拿掉会断连续性，复赛每轮 1 条 / 每订单 1 条
  各扣 5 分）、`delivered[j] − p ≥ need[j]`（不能削到欠产）；另受 50 m 床长下限约束。
- 实测（`--passes 2`）：刀数 185,648 → **181,837**（floor）/ 197,569 → 188,269（intended），
  成材率 91.0793% → 90.5307%，申报钢 549,999.4 → 553,332.2 t，
  **score 91.8000 → 92.3600（+0.5579）**，覆盖率 100%、0 违规。
- ⚠️ **预测偏高**：各 pass 预测 +1.2352 / +0.2965 / +0.0322 / +0.0150 合计 ≈ +1.58，实际只 +0.5579。
  原因是定价是**局部线性近似**（`per_knife`、`per_kg` 在每个 pass 开头冻结），而一次 pass 内
  改写上千轮会把 K 和 R 推离线性化点。**改进方向**：passes 内分段重新定价（每 N 轮 re-evaluate
  一次），而不是每个 pass 只定价一次。
- ⚠️ 整形后**成材率反而降了**（91.08% → 90.53%）：`widen` 引入的超产没有被 `shed` 完全剥掉。
  说明 `shed` 还有空间（`blocked by the 50 m floor` 的轮子需要另一种解法，而不是放弃）。

---

### 0.8 根因（09-22，最大的一条）：**分块求解时，`_key` 把一块当成整批来打分**

#### 0.8.1 现象

同一批 chunk、同一个 `--seed`、同一个 `--chunk 60`，**只差退火**：

| 方案 | 生成方式 | 批数 | 刀数(floor) | 成材率(封顶) | 覆盖率 | **score** |
|---|---|---|---|---|---|---|
| `runs/seed_fixed_all.json` | 纯构造（`_seed_group(deadline=None)`） | 5,000 | 175,731 | 90.7792% | 0.9999 | **93.6511** |
| `runs/semi_nolimit_v1/result.json` | 同种子 + 每块 20 s 退火 | 4,048 | 185,648 | 91.0793% | 1.0000 | **91.7976** |

**退火是负贡献 −1.8534。** 而且 `_key` 自己也认为退火更差（92.3018 / 92.3186 → 90.6983），
所以**不是目标函数写错**，是目标函数被喂了**错的量纲**。

#### 0.8.2 归因：三项子分在块尺度上全部失真

`_key(metrics, cfg, n)` 的 `metrics` 是**这一块**的 `(knives, finished, raw, covered)`，
`n` 是**这一块**的订单数。但 `platform_score` 算的是**整批**的口径。三个项各错一处：

| 项 | 块尺度的实际边际 | 整批的真实边际 | 倍数 | 后果 |
|---|---|---|---|---|
| 刀数 `40·min(1, baseline/K)` | `160000/800 = 200 > 1` ⇒ **恒等 40** | `40·160000/K_tot² = 1.98e-4` | **梯度 = 0** | 刀数完全免费 |
| 成材率 `30·D/R` 的 `∂/∂R` | `30·D_chunk/R_chunk² = 1.078e-5` /kg | `30·D_tot/R_tot² = 4.935e-8` /kg | **218×偏高** | 省料被高估 |
| 覆盖率 `covered/n` | `20/60 = 0.333` /单 | `20/9999 = 0.002` /单 | **167×偏高** | 凑组合被高估 |

`K_chunk` 是块自身的刀数（够大时 `min(1,·)` 就饱和），`R_chunk ≈ R_tot/218`、
`n_chunk = n_tot/167` —— **三项失真的共同根因是把块的分母当成了整批的分母**。

#### 0.8.3 证据 A：单块分解（`diagnostics/key_decompose.py`）

NP01:43 第 600 起的 60 单，20 s：

```
           knives           723.0000 ->  842.0000     +119.0000
       knife_term            40.0000 ->   40.0000       +0.0000   ← 饱和，零梯度
        numerator     2,411,730.6799 -> 2,415,808.2888  +4,077.6  kg
              raw     2,590,624.2880 -> 2,569,821.6600 -20,802.6  kg
       yield_term            27.9284 ->   28.2021       +0.2737
        key gain  +0.2737          GLOBAL gain  -0.0218
```

`_key` 认为赚了 **+0.2737**，整批只掉了 **−0.0218**：
`+119 刀 × 1.86e-4 = −0.0221`，省下 20,803 kg × 30·D/R² = **+0.0010**。
`_key` 把 0.001 的收益当成了 0.27 —— **全是 218× 那条**。

#### 0.8.4 证据 B：整批算术闭合（复现 −1.8534）

```
退火多花 +9,917 刀（floor 口径）   × 40·160000/180000²  = −1.9440
退火多拿 +0.300048 pp 成材率       × 30                 = +0.0900
                                                合计       = −1.8540
实测（93.6511 → 91.7976）                               = −1.8535
```

（成材率分子在复赛是**逐订单封顶**的，整批两个方案的封顶分子**完全相等** = 500,935,598.6
= Σ 每单需求上限，说明两版都刚好满足需求 ⇒ 成材率唯一的杠杆是**降 `raw`**。
退火多花的 9,917 刀换来的是"申报钢坯更省"，但省下来的量被 218× 的错价放大了。）

#### 0.8.5 修法：把整批的口径补进 `_key`

`_key` 里加四个"本搜索看不见的量"（`Config` 字段，默认全 0 ⇒ 与旧算术逐位一致）：

```python
priced_knives   = knives   + cfg.knife_offset
priced_finished = finished + cfg.numerator_offset
priced_raw      = raw      + cfg.raw_offset
priced_covered  = covered  + cfg.covered_offset
total_orders    = cfg.coverage_total or n
score = 40*min(1, baseline/priced_knives) + 30*(priced_finished/priced_raw) \
      + 20*(priced_covered/total_orders) + 10
```

- **刀数必须加在 `min(1,·)` 里面**，不是加在分数上：只有 `min(1, base/(off+K))` 才能把
  偏导从 0 变回真实影子价格 `−40·base/(off+K)²`。
- **成材率分子要按订单封顶**（`solver._capped_finished`）。封顶是**逐批可分**的
  （`validate_plan` 禁止一个订单出现在两个方案里），所以 `search_10s` 的增量
  `current − before + after` 仍然精确。未封顶时退火会去追一个平台**不给分**的收益：
  单块 20 s 让 `_key` `+0.365`，其中 **30×未封顶成材率就是全部的 +0.365**。
- 四个 offset 由 `solve_semi.DropAccounting` 供给：用**已解完的块**的
  "每订单速率" 外推到整批（`SAMPLE_FRACTION = 0.10` 之前用先验，先验取**偏大**值，
  因为让资源显得宽裕（搜索低估它）是安全方向，反过来会重新引入偏置）。
- 缓存键从 `knife_offset` 换成 `score_fingerprint(cfg)`（四个 offset + 封顶开关 + 总单数），
  否则 `none` 和 `auto` 两个臂会互相复用对方的块，**把两个目标函数混进一个方案里**。

#### 0.8.6 ⚠️ 两个必须知道的副作用

1. **退火对时钟敏感 ⇒ 逐块 A/B 的每一臂都只是单次采样**。`temperature` 与 `deadline`
   都取 `perf_counter()`，同样 `--seed` 跑两遍**结果不同**。实测：同一块同样种子，
   `cap` 臂两次分别得到 global_K `+88` 与 `+110`。**做 A/B 必须多块取均值**
   （`diagnostics/chunk_global_ab.py`：固定背景 + 多块 × 多臂，输出 `183× mean` 投影）。
2. **`search_10s` 的返回值曾被 `_export` 丢掉 `covered_shared`**：`_export` 不写这个字段、
   `_import_plan` 也不还原 ⇒ 任何走 JSON 往返的路径都把覆盖率**记成 flat 口径**
   （`Batch.metrics` 的 fallback：`len(ids) if len(ids)>1 else 0`）。
   `solver.py` 里 **`covered_shared` 从未被赋值过**（只有 `Batch.metrics` 在读它），
   即**内存里的覆盖率也一直是 flat 口径**，而 `validate_plan` / `platform_score` 用 shared 口径。
   本次修复只处理了 `_key` 的**分母**（`coverage_total`），**分子口径仍未对齐**——
   flat 会把"多订单方案里没共享轮次的订单"也算成已覆盖，于是退火有动机
   **合并方案**（批数 5,000 → 4,048）。见 §13 待办。

---

### 0.9 计刀口径终于定案（09-22）：**平台按 floor 记，`_key` 必须跟着 floor**

#### 0.9.1 §0.4 里那个"未定"的答案是 **floor**

证据是 `entry_knives` 文档串里那次 09-16 的 92.02 官方反馈，重新读一遍它其实已经给出了答案：

> 那发方案**故意**把每个长度写成略微低于整数倍（相对 1e-12），于是 `L // size` 塌成 `k-1`。
> 官方回了 108,671 刀 / 35,718 段 = **Σ(k≥2 的 k) + 2 × #{k==1}**。

关键不是"补回"，而是**官方确实是用 `int(L // size)` 去数段的**——因为 `entry_knives = max(2, int(L//size)+1)`，
只有当官方自己算出 `k-1` 时，才会得到 `max(2, (k-1)+1) = max(2, k)`，也就是
`Σ_{k≥2} k + 2·#{k=1}`。**如果官方用 snap/`round` 记账，这个式子根本对不上。**
§0.4 里"初赛反馈支持 snap"那句是当时的反向读法，**已被推翻**。

复赛的计刀规则（PDF 六）是 `Σ int(L // size) + 1`，没有 `max(2,·)` —— 同一个 `//` 语义，
所以**复赛也按 floor 记**。

#### 0.9.2 两套口径的差额，以及为什么它能把整形器搞崩

`_export` 写的是 `round(k*size + trim, 9)`；平台对它做 `int(L // size)`：

| 口径 | 定义 | 纯构造方案的刀数 | score |
|---|---|---|---|
| intended | `Σk + 1`/轮（求解器内存里的 `Round.knives`） | **186,376** | 92.30 |
| **platform floor** | `Σ int(L // size) + 1`/轮 | **175,731** | **93.65** |

差 **10,645 刀 = 6%**。而且 **这个映射对 `k` 不单调**：改写轮次会改变"哪些 `k` 会塌"，
于是 `Σk` 下降可以让**被判分的刀数上升**。

**`reshape_plan.py` 正是这么翻车的**（`runs/seed_reshape.log`）：

```
pass 1: 预测 +0.0525   实际 -0.8076   (93.6511 -> 92.8400)
   floor 口径: knives 175,731 -> 179,694     ← 被判分的刀数 +3,963
   intended : knives 186,376 -> 186,117     ← 它自己看到的 `Σk` −259
```

整形器按 intended 优化，被判分的 floor 反向走了 3,963 刀，**净亏 0.8063 分**。
⇒ **在 floor 口径修好之前，不要再用 `reshape_plan.py`。**

#### 0.9.3 修法：`Config.knife_convention`

```python
def platform_segment_count(k, size, segment_trim):
    return int(round(k * size + segment_trim, 9) // size)   # 复刻 _export 的浮点往返
```

`make_round` 在 `knife_convention == 'platform_floor'` 时用
`Σ platform_segment_count(k) + 1` 当 `Round.knives`；默认 `'intended'` 保持旧行为，
所有既有测试与初赛路径不变。`solve_semi.py` 设 `platform_floor`。

#### 0.9.4 最终对齐结果（**逐位相同**）

对齐后 `_key` 的内存度量与 `platform_score` 完全一致（`runs/seed_fixed_all.json`）：

| 量 | `_key`（内存） | `platform_score` |
|---|---|---|
| knives | 175,731 | 175,731 |
| 成材率分子 | 500,935,598.6 | 500,935,598.6 |
| raw | 551,817,305.8 | 551,817,305.8 |
| 覆盖订单 | 9,998 | 9,998 |

（过程中还抓到一个 1.1% 的偏差：成材率分子必须用 `Order.linear_weight_int`
——**直径截断到整数毫米**的重量，即 `platform_score` 的口径。用 `linear_weight`（精确直径）
会把分子报成 506,504,659 而不是 500,935,598.6。）

⇒ 加上 §0.8 的四个 offset 之后，**`_key` 就是全局 `platform_score` 的一个精确代理**，
搜索的每一步都在优化真正被评判的那个函数。

---

### 0.10 四个 offset 从哪来（09-22）：**从一份现成方案里读，不要估**

§0.8 把 offset 的**位置**修对了，但 offset 的**数值**还有一个独立的坑，而且它同样很大。

#### 0.10.1 先验的口径必须和它喂进去的量一致

`DropAccounting`（`--offset-mode share`）在解完的订单不足 `SAMPLE_FRACTION=10%` 之前
只能靠先验。第一版先验写的是 `knife_prior=190000`（"一根坯料约 1.9 刀"的粗略估计），
但 `knife_offset` 要加进的是**按 `platform_floor` 口径测出来的刀数**，而整批 floor 口径实测是
**175,731**。190,000 / 175,731 = **+8.1%**。

这个误差不是中性的，因为它**重新缩放了一个影子价格**：

```
∂/∂K [40 · min(1, 160000/(K+o))] = −40 · 160000 / (K+o)²
  o = 174,980（真值）→ 每刀 2.094e-4 分
  o = 188,860（冷启动估出来的）→ 每刀 1.777e-4 分
```

**offset 高 8%，每把刀就显得便宜 15%**，退火又会拿刀去换成材率——就是 §0.8 要修的那个病。
同一份诊断里 `raw` 也偏（0.90 的中点 vs 实测 0.90834，`R` 高报 0.80%），方向是安全的，
但同样说明**估出来的 offset 会悄悄改掉搜索的目标**。

> 教训：offset 是"整批的口径"，它自己必须**用整批的口径测量**。拿一个别的口径的
> 数量级估计去填，等于换了个目标函数。

#### 0.10.2 修法：`AnchorOffsets` / `--offset-mode anchor`（新默认）

既然手上已经有一份**全量、同口径**的方案（`runs/seed_fixed_all.json`），就不需要估：

一块订单**看不见**的东西 = 这份方案里**不与它共享任何订单**的那些批。四个 offset 就**是**
这些批的度量。**注意不是 `整批 − 这些批`**——那样算出来的是这块自己那几百刀，恰好反过来：

```
IDENTITY: offset + chunk-in-anchor == whole ?   (chunk = NP01:43 前 60 单)
  knives     off    174980.0000 + chunk     751.0000 =    175731.0000  vs whole    175731.0000  Δ 0
  numerator  off 498260095.7502 + chunk 2675502.8264 = 500935598.5767  vs whole 500935598.5767  Δ 0
  raw        off 548880390.4640 + chunk 2936915.3760 = 551817305.8400  vs whole 551817305.8400  Δ 0
  covered    off      9938.0000 + chunk      60.0000 =      9998.0000  vs whole      9998.0000  Δ 0
```

四项（含浮点分子）**逐位闭合**，且与 `diagnostics/chunk_global_ab.py` 的 `exact` 臂
`bg` 完全一致。各量对**不相交的批**都是可加的（刀数、覆盖按方案计，交付量按订单求和），
所以这是**恒等式而非近似**；不在参照方案里的块，背景自然退化成整份方案，offset 取满，也对。

它**不是**抄参照方案：块仍然是从零重解，参照只提供影子价格。
实现位于 `solve_semi.py` 的 `AnchorOffsets`，`--offset-anchor` 默认指向
`runs/seed_fixed_all.json`。

#### 0.10.3 三臂 A/B（`runs/offset_ab_v3.log`，6 块 × 20 s）

| 臂 | 每块均值 | ×183 投影 | Δ刀数/块 | Δ成材率/块 |
|---|---|---|---|---|
| `seed`（参考线） | 0.0000 | 0.000 | 0 | 0.0000 pp |
| `none`（修复前目标） | **−0.0124** | **−2.261** | **+40 ~ +81** | −0.0014 ~ +0.0075 pp |
| `exact`（= `anchor`） | **+0.0018** | **+0.329** | **−3 ~ −32** | −0.0065 ~ +0.0005 pp |
| `exact − none` | +0.0142 | +2.59 | — | — |

机制被证实：`none` 臂**系统性多花刀**（6 块全部为正）换取成材率，整批算下来亏分；
`exact` 臂**系统性少花刀**，成材率略降，净分上升。

> ⚠️ `183 × mean` 是逐块独立假设下的外推，**只用来定方向和量级**；
> 单块极差 `−0.0079 ~ −0.0164`（`none` 臂）也印证了 §0.8.6 的"时钟敏感、单次采样不足"。

#### 0.10.3b `share` 臂：**先验修对之后，与 `exact` 逐位同分**（`runs/offset_ab_share.log`）

同样 6 块 × 20 s，只跑 `seed` 与 `share` 两臂（先验已改成 175,731）：

| 块 | `exact` | `share` |
|---|---|---|
| NP01@0 | 93.6543 (−18) | 93.6543 (−18) |
| NP01@600 | 93.6534 (−32) | 93.6534 (−32) |
| C60@0 | 93.6536 (−3) | 93.6536 (−3) |
| C60@600 | 93.6551 (−18) | 93.6551 (−18) |
| 30C@0 | 93.6541 (−4) | 93.6541 (−4) |
| 30C@600 | 93.6543 (−10) | 93.6543 (−10) |
| **均值 / ×183** | **+0.0018 / +0.329** | **+0.0018 / +0.329** |

**六行全同，连 worst/best 都一样**。算术上本该如此：冷启动 `share` 的刀数是
`175731 × (1 − 60/9999) = 174,677`，真值 174,980，差 **−0.17%**
——比退火在 20 s 内的分辨率还小。

⇒ **§0.10.1 那个 +7.9% 完全是旧先验的口径错误，不是"估"这个方法本身有毛病。**
三个推论：

1. 有全量参照方案时仍用 `anchor`：它**按构造精确**，不依赖任何常数，也不会随块漂移。
2. **没有参照方案的第一遍（新 drop）可以放心用 `share`**，只要先验用**被测口径**的实测值。
3. 两者在本 drop 上不可区分，所以**不要在它们之间做无谓的 A/B**——真正的杠杆是
   §0.10.1 那个先验的**口径**（+8.1% ⇒ 每刀虚便宜 15%）。

---

### 0.11 多岛 + 逐块择优：**块间无耦合，所以「取每块最好的」是严格安全的**（09-22）

#### 0.11.1 为什么可以逐块择优

`solve_semi.py` 把每组按 60 单切片，一次只解一块；**块与块之间订单不相交、方案不相交**，
所以两份方案之差**只**体现在"同一块解出了什么"。块的贡献对整批度量是**局部的、可加的**，
因此**对每一块独立挑最好的那份**是严格的（不是启发式）。

**关键**：候选之间用 `_key` 比较是**精确的**，因为同一块在不同岛里拿到的
**offset 完全相同**（同一个 anchor、同一段订单切片）⇒ offset 在比较中**约掉**，
`_key` 等于按唯一的全局目标排序。**跨块比较则不成立**（offset 不同），
所以择优**只能逐块做，绝不能全局做**。

#### 0.11.2 算力是免费的

本机 **24 核**，而退火是**单线程**（`solver.py` 里没有 Thread/worker）。
⇒ 想开几个岛就开几个岛，互不干扰。当前跑法（`--offset-mode anchor --chunk 60`）：

| 岛 | 命令 | 每块预算 | 预计耗时 |
|---|---|---|---|
| s1 | `--seed 1` | 20 s | ≈65 min |
| s2 | `--seed 2` | 20 s | ≈65 min |
| s3 / s4 | `--seed 3` / `--seed 4` | 20 s | ≈65 min |
| deep | `--seed 1` | 60 s | ≈3 h |

换种子即可（驱动器里 seed 是 `args.seed + offset`）。

#### 0.11.3 合并器 `diagnostics/merge_chunks.py`

```bash
python diagnostics/merge_chunks.py \
    --runs runs/semi_anchor_v1 runs/semi_anchor_s2 runs/semi_anchor_60 ... \
    --output runs/semi_merged
```

- 逐块在「各岛的块方案」中取 `_key` 最大者；**没解出来的块用 anchor 回填**
  （未放置订单就是平台在打的覆盖率），回填**只取完全落在该块内的 anchor 批**——
  跨界批会把一个订单塞进两个方案，出现时记进报告而不是硬塞。
- 打分必须走和 `search_10s` **同一条算术**：`_totals` 吃的是 `Batch` 对象**不是**导出的 dict，
  所以是 `S._totals(S._import_plan(plan, model), model)`。
  （与 `AnchorOffsets.metrics` 对过账：knives 175,731、覆盖 9,998 **完全一致**，
  numerator/raw 只差浮点结合律。）
- **别用 `<run>/result.json` 的 `summary.json` 当验收**：那是**按块**口径的自报数，
  验收一律 `platform_score.evaluate(merged, ...)`。

#### 0.11.4 实测（`runs/semi_merged`）

| 指标 | 16 块解出时（冒烟） | **31 块解出时** |
|---|---|---|
| 块胜负 | v1 14 / s2 1 / deep 1 | v1 20 / s2 4 / s4 2 / deep 5 / s3 0 |
| 回填块数 | 167 | 152 |
| 跨界 anchor 批 | **0** | **0** |
| 每块 `_key` 增益（对 anchor） | +0.003796 | **+0.004497** |
| ×183 投影 | +0.695 | **+0.823** |
| `platform_score` | 93.7120（+0.061） | **93.7914**（**+0.140**） |
| 刀数 / 覆盖率 / 违规 | 175,351 / 0.9998 / 0 | **174,845** / 0.9997 / **0** |

⇒ 逐块择优把单候选的 +0.0018/块 抬到 **+0.0045/块**（取 max 的收益），
投影终值 **≈93.65 + 0.82 ≈ 94.47**，可压过已交保底的 **93.9160**。
**60 s 的深搜岛在 31 块里赢了 5 块** ⇒ 多给退火时间还有边际收益，见 §0.12。

---

### 0.12 精修一轮：把「择优结果」当初始解再退火（09-23）

逐块择优只会在**已解出的候选之间**挑，本身不再搜索；而 §0.11.4 里
**60 s 的深搜岛在 31 块里赢了 5 块**，说明「多给退火时间」还有边际收益。
两者可以叠加：**把择优结果当作初始解，再退火一轮。**

`solver.search_10s` 本来就支持 `initial_plan`：给了它**就跳过播种**，
直接把导入的批灌进 `current`，`current_metrics` 由它们算出，
`best` 从这份初始解开始往上走 ⇒ **这一轮在构造上单调**（不可能返回比交给它的更差的东西）。
`solve_semi.py` 新增 `--refine <全量方案.json>`，逐块切出「完全落在该块内」的批传下去。

#### 0.12.1 实测单调性（`search_10s(..., seconds=20, initial_plan=merged 块)`）

| 块 | 精修前 `_key` | 精修后 `_key` | Δ |
|---|---|---|---|
| NP01:43@0（岛已解过） | 93.657432 | 93.657765 | **+0.000334** |
| C60:26.5@0（回填的） | 93.651074 | 93.652342 | **+0.001268** |
| 30C:45.0@0（回填的） | 93.651074 | 93.653983 | **+0.002909** |

三块**全部为正**，且**连岛已经解过的块还能再挤出 +0.0003**
⇒ 「择优」与「精修」不是一回事，**要串起来做**。

#### 0.12.2 用法与两个坑

```bash
# 必须先合并出一份全量方案
python diagnostics/merge_chunks.py --runs <各岛> --output runs/semi_merged
# 再以它为初始解精修；60 s/块 ⇒ 183 块 ≈ 3 h
python -u solve_semi.py --output runs/semi_refine_v1 --chunk 60 \
    --seconds-per-chunk 60 --budget 21600 --offset-mode anchor \
    --refine runs/semi_merged/result.json
```

1. ⚠️ **必须用新的 `--output`**：块缓存（`chunks/<group>/<offset>.json`）的键是
   `score_fingerprint`（只含 offset / 封顶开关 / 计刀口径 / 总单数），**不含初始解**
   ⇒ 与 from-scratch 的岛共用目录会互相复用缓存，把两种来源的块混在一起。
2. ⚠️ 初始解**只取完全落在块内的批**：跨界批会引用该块 `Model` 里不存在的订单，
   `_import_plan` 会越界索引。

⇒ 完整流水线：**多岛并行 → `merge_chunks.py` 逐块择优 → `--refine` 精修 → 再择优 → 打包**。

#### 0.10.4 缓存键必须包含 offset

`solve_chunk` 的缓存判定键是 `score_fingerprint(cfg)`（四个 offset + 封顶开关 + 计刀口径 + 总单数）。
改 offset 而没进这个键，就会让**不同目标函数的块互相复用**——§0.8.5 里已经踩过一次，
两个臂混进同一个方案里。换 `--offset-mode` 必须换 `--output` 目录，或依赖这个指纹。

---

## 1. 初赛 vs 复赛：总差异表

| 维度 | 初赛 | 复赛 |
|---|---|---|
| 超产 | 有惩罚规定，但**评分脚本惩罚代码有误 ⇒ 实际视为没有惩罚**（09-16 17:01） | **改为欠产惩罚**（少生产扣分）；**超产不扣分，但超产部分不计入成材率** |
| 成材率分子 | **包含**超产成品 | **不包含**超产成品 |
| 成材率分母 | 各轮申报钢坯重量之和 | 同左（各轮申报钢坯重量之和） |
| 组合覆盖率 | 同一批内不同轮次切过即可 | **收紧：必须同一轮次共同上冷床才算有效组合** |
| 订单连续性 | 无约束 | **复赛有连续相邻约束**，跳轮即违规 |
| 计刀口径 | 评分脚本**给每个订单都加了头尾**（口径有问题） | **修正**：连接处算一刀，**每轮冷床只有一组切头切尾** |
| 轮数上限 | 不限 | **每组合方案 ≤6 轮**（`constraints.txt` 第 4 条已确认为 6） |
| 时间子分 | 独立 10% 子项 | **并入成材率子项**（09-14 11:32）；但"求解时间"本身已作废，见 §0.1 |
| 单批求解时限 | PDF 五(三)：**≤10 秒** | **作废**：复赛/半决赛无须考虑求解时间（09-21，§0.1） |
| 刀数基准 | 90000（09-18 08:27） | **160000**（09-21 15:46，§0.2） |
| 未计算订单 | — | **所有非异常订单均须给出锯切方案，否则每单计 1 次违规** |

### 1.1 被赛题组自我修正过的说法（别照抄旧结论）

- **超产惩罚**：先说"初赛惩罚超产"（09-15 11:11）→ 后改"评分脚本中该惩罚代码有误，
  实际可视为没有"（09-16 17:01）。
- **组合订单定义**：先说"同一批里不同轮次切过就算"（09-14 11:32）→ 后改
  "**必须在一轮冷床为 2 个及以上订单分配了长度，对应订单才算组合订单**"（09-14 15:45）。
- **计刀**：先说初赛口径（每订单加头尾）→ 明确复赛会**修正**为连接处一刀 + 每轮一组头尾
  （09-15 11:11、09-18 08:27）。
- **晋级门槛**：先说"分数过 60 就行"（SUBARU AWA）→ 更正为"**低于 60 分不予授奖**"，
  进复赛只要求"提交一次非 0 有效成绩"（09-14 17:02~17:06）。

---

## 2. 成材率

- **分子**：成品重量。**初赛包含超产成品；复赛不包含**（09-16 17:01）。
- **分母**：**各轮申报钢坯重量之和**（09-15 11:11、09-16 17:01、09-17 08:37）。
  - 两端各 1m 固定切损对应的棒材重量**计入**承重与钢坯需求校验，**也进分母**（09-17 08:37）。
- 每轮坯料重量**不低于棒材重量**（09-18 08:27，属"轮次坯数下限校验"的答复）。
- 计算时间子分**并入成材率子项**（复赛，09-14 11:32）。

## 3. 计刀（最容易被改坏的一项）

### 3.1 复赛口径（09-15 11:11、09-18 08:27）

- **两个订单连接处算一刀；每轮冷床只有一组切头切尾。**
- **锯切刀数不乘棒材根数。**
- 推论（可直接用于实现）：一轮冷床的刀数 = **总段数 + 1**。
  - 例：同一根棒材先切 A 5 段、再切 B 3 段（共 8 段）→ **9 刀**（内部 7 + 两端 2）。
  - 例：同一轮 A 切 10 段、B 切 5 段（共 15 段）→ **16 刀**（09-16 17:01 明确答复"应该是 16 刀"）。
- 初赛口径**每个订单都加了头尾**（即每订单 +2 刀），这是脚本缺陷，复赛修正。
- **子项得分有封顶**；**初赛刀数基准 9w（90000）**（09-18 08:27）、
  **复赛刀数基准 16w（160000）**（09-21 15:46）。复赛实测刀数能否压到 16w 以内，
  决定刀数子项是否恒为满分，见 §0.2。

### 3.2 本项目受影响之处

现有 `costs=[0]+[max(2,k) …]` 与 `value()` 的按订单累加，是**按初赛口径校准的**。
复赛必须改成"每轮一组头尾 + 连接处一刀"。详见 §12。

## 4. 组合覆盖率

- **有效组合 = 必须与其他订单共同上过同一轮冷床**（09-14 11:32、09-14 15:45）。
- 示例（09-16 14:06 问 → 14:06/14:09 答）：
  - 两轮分别为 `A`、`B`，A 与 B **从未同轮** → 初赛 A 属于组合订单；**复赛 A 不计入有效覆盖**。
  - 两轮 `A+B`、`B+C` → **B、C 计入有效覆盖**。
  - 三轮 `A+B`、`B+C`、`A+C` → A 在第 1、3 轮出现而第 2 轮缺失 → **违反连续性**。
- **跨冷床连续性也要考虑**（09-14 11:32）。

## 5. 连续性 / 相邻约束（复赛新增）

- 订单在轮次序列中出现必须**连续**，不能跳轮。
- 违规示例：第 1 轮生产 A；第 2 轮只生产 B；第 3 轮继续 A → **属于违规**（09-15 11:11）。
- 违规示例（轮次视角）：`1,2,3`；`2,4,5`；`3,4` → **不允许**（3 不连续）（09-14 14:37~14:38）。
- **计数口径**：**连续性按轮次计，跳轮按订单计**（09-16 14:09）。
- "订单完成后停止生产是否允许" → 赛题组未答（回"4 什么意思"），**待确认**。

## 6. 违规与惩罚

- **所有约束违反都以惩罚项形式给出**（09-14 14:52）。
- **每条违反从总分里扣 5 分**（09-14 14:59）。
- 未计算订单：**每条未计算订单计为一次违规**（09-14 11:32），即同样按 5 分/条扣。

## 7. 长度与重量口径

- **单位是米**（09-14 11:32）。
- **`length_scheme` 只包含分配给该订单的部分，不含切头切尾**（09-15 11:11、09-16 17:01）。
  **切头切尾不用写在解里，评分脚本会自动加上进行辊道长度校验**（09-20 16:10）。
- **`length_scheme` 写"每根棒材上分配给该订单的长度"，不是本轮总长**（09-20 16:20/16:23）：
  - 例：本轮订单 a 用了**两根 140m** 棒材 → `length_scheme` 写 **140**，**不是 280**。
  - 原话："length_scheme 是并排的所有棒材的对应长度都分配给这个订单，不是一根一根分的"。
- **题目约束（辊道 150m 上限、60t 承重）按棒材总长度计算**（09-15 11:11）。
  - 承重针对**实际放上辊道的棒材**；钢坯余量**不允许跨轮**（09-16 17:01）。
- **两端切损固定 1m，不能延长**（09-14 15:46）→ 不能靠加长切损来把不足 50m 的轮次凑到 50m。
- **浮点容差无需自保**：不会因为计算误差少算一段，评分脚本已考虑编程语言误差（09-15 11:11）。

## 8. 冷床 / 轮次语义

- **一轮冷床 = 一个 scheme**；不存在一轮冷床多个 scheme；**每组分配方案可对应多轮冷床**
  （09-14 11:32，与赛题 PDF「四、（四）输出结果」一致）。
- **一个订单最多分几轮：不限**（09-15 11:11）。
- **一组方案最多轮数：初赛不限；复赛每方案上限 6 轮**，`constraints.txt` 第 4 条原文
  「每个订单组合方案使用的冷床轮数不得超过6轮」（09-15 11:11 问，09-21 由 `constraints.txt` 确认）。
  **注意这是「每方案」预算而非「每订单」**：重订单可独占耗尽 6 轮，故两单能否共享一个方案
  取决于 `rounds_min(A) + rounds_min(B) <= 6`（`solver._can_share` 的判据）。

## 9. 钢坯 / 余料

- **不能跨轮复用**：每轮冷床使用的铸坯仅可用于本轮冷床（09-15 11:11、09-16 17:01）。
- 每轮坯料重量不低于棒材重量（09-18 08:27）。

## 10. 异常订单

- **异常数据在数据清洗时跳过**（09-14 11:32）。
- 复赛要求**所有非异常订单**均须计算锯切方案（09-14 11:32）。

## 11. 非技术规则（赛程）

- **晋级复赛**：初赛必须**提交至少一次有效结果**，排行榜能查到分数（0 分也可）；
  **没有提交记录无法进入复赛**（09-20 10:00 群公告、09-14 16:59~17:02）。
- **授奖门槛**：提交成绩**低于 60 分不予授奖**（09-14 17:06）。
- **初赛只提交结果文件，不提交技术报告**（09-16 17:58）。
- 报名费 500 元，通知上有（09-15 14:24）。同一赛题每位选手只能加入一支团队；可跨赛题
  （09-15 15:06）。
- 初赛提交截止 **09-20 20:00**（报名 19:00 截止）。**决赛线下**（09-14 16:35）。
- 复赛通知：`www.aicomp.cn/notice/notice-3/5248.html`。
- **复赛数据集已发布**（09-21 11:13~11:14）：在"我的参赛信息"→"详情"中查看网盘链接。
- **参赛成员在初赛截止后不可变更**（09-21 09:28："不能"加新队友）。

### 11.1 复赛赛程（09-21 核实自官方通知原文，**优先于以上口语转述**）

来源：`www.aicomp.cn/notice/notice-3/5248.html`（全智赛组委会〔2026〕37号）

| 节点 | 时间 |
|---|---|
| 复赛数据集开放 | **09-21 11:00** |
| 复赛结果文档**提交开始** | **09-22 09:00** |
| 复赛结果文档**提交截止** | **10-05 20:00** |
| 复赛**代码**提交截止 | **10-07 23:59** |

- **每日提交次数上限 5 次**；**取复赛阶段最高成绩为最终成绩**（提过的分不会掉）。
- **初赛成绩不计入复赛及后续赛段总成绩**，仅作晋级依据。
- **排行榜非实时更新**（算分有延迟），官方提示"避免截止前卡点提交，预留充足算分时间"。
- **复赛须提交核心代码文件、模型文件与环境**（非仅结果文档），评审会做**结果复现与审核**，
  可要求团队解释说明。
- 复赛基准线 **60 分**；一、二等奖获奖作品推荐参加**半决赛**。

> ⚠️ 上文"复赛数据集开放后一天开始提交"是赛题组口语转述（09-15 11:14），
> 与官方通知一致（21 日开放 → 22 日开始），但**具体时刻以本表为准**。

### 11.2 复赛提交机制（09-21 群公告与答疑，**提交前必读**）

- **两个提交入口，别走错**：**预测文件**（结果压缩包，与初赛同形式）与**作品文档**分别提交。
  官方原话："预测文件、作品文档提交入口不一样，不要搞错"（09-21 14:27 群公告）。
- **每天只有 5 次提交机会**（09-21 14:27 群公告）。
- **需要核验的代码放网盘**，不走提交入口（09-21 15:36）；"最后的作品再以网盘链接的形式提交"。
- 提交自 **09-22 09:00** 开始（09-21 14:27"明天 9 点开始作品提交"）。
- 具体要求以复赛通知**附件 2《作品提交要求》**为准；代码入口"看赛道提交要求"（09-21 12:44）。
- 结果 CSV 的字段与行结构**官方未给示例**（09-21 12:34 提问未获答复），
  以初赛格式与赛题 PDF「四、（四）输出结果」为准。

---

## 12. 对本项目代码的影响（复赛必须改，初赛可不动）

按"会被打破"的严重程度排序：

| # | 现状 | 复赛要求 | 需改 |
|---|---|---|---|
| 1 | **计刀按订单各加头尾**（`max(2,k)` 累加，初赛口径校准） | 每轮**一组**头尾 + 连接处一刀 ⇒ 一轮刀数 = 段数 + 1 | `platform_score.row_metrics` / 引擎 `costs` / `value()`。**这是最大改动，且会改变所有历史校准点** |
| 2 | `coverage` 按"同 batch 内出现"计 | 必须**同轮次共同上冷床**才算有效组合 | `platform_score.evaluate` 的 `combined` 判定 |
| 3 | **无连续性约束** | 跳轮违规，**每轮次 1 条 / 每订单 1 条**，各扣 5 分 | `validate_plan` + 方案生成（轮次排列） |
| 4 | 支持**≤100 轮**（已提交包有恰 100 轮方案） | 复赛 **每方案 ≤6 轮**（`constraints.txt` 第 4 条） | `build_submission.merge_compatible` 的装箱上限；方案生成须先出 ≤6 轮的紧凑结构 |
| 5 | `max_overproduction_ratio=0` ⇒ `caps[i]=pieces`，**要求交付精确等于需求**，连超产也判违规 | 初赛脚本超产惩罚**失效**；复赛**欠产惩罚**、**不设超产上限**（09-16 17:01），超产**不扣分**但**不计入成材率分子** ⇒ 合法区间应为 `delivered ≥ demand` | 放宽 `max_overproduction_ratio`；`validate_plan` 的 `[pieces, caps]` 判据；`_solo_scheme.try_shape` 的 `total_k*parallel == pieces` 整除判据可放宽为 `≥`；成材率分子取 `min(delivered, demand)` |
| 6 | `time` 子分固定 100 | 时间分**并入成材率子项** | `score_components` 权重 |
| 7 | `length_scheme` 已是"每根棒材上的分配长度"、不含切损 | 与复赛口径**一致，无需改** | — |
| 8 | 钢坯 per-round、不跨轮；切损固定 1m 不可延长；50m 下限 | 与复赛**一致** | — |
| 9 | `baseline_knives` 处处按 **90000** 设（初赛基准） | 复赛基准 **160000** ⇒ 刀数子项更容易封顶，实际权重移向成材率 / 覆盖率（§0.2） | `data/semi/competition.config.json`、`solve_semi.py` 的 `settings.update(...)`、`_key` 的比较优先级、所有对照脚本 |
| 10 | 单批求解按 **≤10 秒** 预算（`search_10s(seconds=)`） | **无时限**（09-21，§0.1）⇒ 预算不再是约束，应把时间全部换成绩效 | `solve_semi.py` 的 `--seconds-per-chunk` 与 `--budget` 大幅放大；`search_10s` 的退火预算随之放开 |

**验证策略不变**：改完继续用 `test_solver.py` 的独立小实例枚举对拍，并让
`python -X utf8 -m unittest -v test_platform_score` 全过。

## 13. 仍待确认（赛题组未答复）

1. **辊道超重判定依据**：JSON 里只有单根棒材长度与轮次根数，重量如何算出（09-20 17:17 提问，
   截至 09-20 未答）。
2. **订单完成后停止生产是否允许**（09-16 14:06 提问，赛题组回"什么意思"，未答）。
3. **"自测 9.1w 系统返回 9.5w"** 的口径差（09-18 10:40 提问，未答）——疑似初赛脚本
   每订单加头尾导致的偏差。
4. ~~**复赛 `constraints.txt`** 的轮数上限具体值（5 还是 8）。~~
   **已确认：每方案 6 轮**（`constraints.txt` 第 4 条，09-21）。权重口径见第 6 条。
5. ~~复赛数据的确切开放时间（"明天吧"，09-20 11:05）。~~
   **已发布：09-21 11:13~11:14**，在"我的参赛信息"→"详情"中查看网盘链接。
6. **「求解时间作废」是否波及评分表的「时间子项」**：§0.1 已明确 PDF 五(三) 的 10 秒硬约束
   作废，但评分权重里那 10% 的"时间"**是否同样取消，未获答复**。若取消并并入成材率，
   成材率权重由 0.3 → 0.4，优化重心进一步偏向成材率。
   `Rules.semi()` 现仍按 PDF 九实现（`time_subscore` 独立项），`--weights` 可覆盖以做对照。
7. **成材率与刀数是否等权（各 0.4）**：09-21 15:47 有选手如此追问，**官方未答**，属选手推测，
   不得作为依据。
8. **复赛结果 CSV 的字段与行结构**：09-21 12:34 提问未获实质答复（官方回"代码入口看赛道提交要求"）。
