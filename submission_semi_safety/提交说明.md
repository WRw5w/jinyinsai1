# 复赛保底提交（第二台机器的方案）

**上传文件：`复赛结果_棒材优化.zip`**（122,000 字节）
提交入口：**预测文件**入口（结果压缩包），**不是**作品文档入口。见 `RULES.md §11.2`
—— 官方 09-21 14:27 群公告原话「预测文件、作品文档提交入口不一样，不要搞错」。

## 为什么先交这一发

- 复赛**每天 5 次提交机会**、**取复赛阶段最高成绩**为最终成绩（`RULES.md §11.1`）
  ⇒ 先交一发保底**不会**让后续更好的提交掉分。
- 提交窗口 09-22 09:00 ~ **10-05 20:00**，排行榜**非实时**（算分有延迟），
  官方提示「避免截止前卡点提交」。
- ⚠️ 这一发是**第二台机器**的方案，不是本 fork 自己的产物。作为保底可用；
  若要交自己路线的东西，用本 fork 的 `runs/reshape/ours_r2.json`（见文末对照）。

## 本次独立复核（本 fork 的 `platform_check` / `platform_score`，不是对方自报）

| 项 | 值 |
|---|---|
| `platform_check` | **passed = true**，0 errors，0 warnings |
| 方案数 | 2,196 |
| 轮数 | 12,559 |
| 订单 | 9,999 / 10,000（剔除无效单 `B20270281`，与本 fork 的 `skip_invalid` 一致） |
| 每方案轮数上限 | 6（实测 max = 6，无超限） |
| 违规数 | **0** |
| 组合覆盖率 | **99.99%** |
| 成材率（官方口径：超产不计入分子） | **91.8081%** |
| 刀数（floor 口径 `int(L // size)`） | **175,952** |
| 预测总分 @ 基准 160,000 | **93.9100** |
| 预测总分 @ 基准 90,000 | 78.0000 |

> 基准取 **160,000**：`RULES.md §0.2` 记赛题组 09-21 15:46 官方答复。
> 对方自己没收到那次更新（其 `RULES.md` 仍写 90,000），但**这不影响方案合法性**，
> 官方算分用哪个基准是官方的事。

## 哈希（提交前后各核一次）

```
复赛结果_棒材优化.zip
  sha256  52a1b004011c481204a1cd291b69aa6532c777554d95367f1c86a10dc3e8fb52
  122,000 bytes

包内唯一文件 复赛结果_棒材优化.json
  sha256  87b9812ee96cea60f6fabd020bc13bd87fb083401777ba072aedf2ec0e6e1fa4
  803,524 bytes
```

- **包内字节与磁盘 JSON 逐位相同**（已复验；`RULES.md` 的教训：长度表示影响计刀，
  **包内才是权威**，别拿磁盘 JSON 的数字当提交数字）。
- 包内两个哈希与对方 `validation_report.json` 里的 `json_sha256` / `zip_sha256`
  **完全一致**，是相互独立算出的同一结果。

## 格式核对

- ZIP 根目录**单个** JSON，无子目录 —— 与初赛那发拿过官方 92.02 的
  `submission_island_partition/初赛结果_棒材优化.zip` 结构一致（唯文件名换成「复赛」）。
- UTF-8 **无 BOM**，首字节 `[`，顶层是数组。
- 单方案字段：`orders` / `length_scheme` / `counts` / `blank_type` / `blank_counts`。

## 未决 / 需要知道的风险

1. **`official_acceptance_verified: false`** —— 对方也没拿到官方确认，这份是自测通过，
   不是「官方已判可行」。
2. **计刀口径 floor vs snap 未定**（`RULES.md §0.4`）。本表的 175,952 / 93.9100 用的是
   **floor**。若官方按 snap 记账（两套独立实现都按 snap 自报），对方是 **190,341** 刀、
   预测分约 **91.17**。⇒ 真实分落点更可能是 91~94 之间，别把 93.91 当保底数。
3. 覆盖率 **99.99% 而非 100%**（20 分项少约 0.002 分），不是缺陷，是那一个无效订单的边界效应。

## 本 fork 自己的候选（若要用自己的）

| | 现役 `runs/semi_nolimit_v1/result.json` | **整形后 `runs/reshape/ours_r2.json`** |
|---|---|---|
| 刀数(floor) | 185,648 | **181,837** |
| 成材率 | 91.0793% | 90.5307% |
| 覆盖率 / 违规 / `platform_check` | 100% / 0 / true | 100% / 0 / true |
| 每方案轮数上限 | 6 | 6 |
| 预测分 @160k (floor) | 91.80 | **92.36** |

两者**都还低于**本次保底提交的 93.91 —— 所以先交这一发是对的。
`semi_fixed_v1`（修复版全量重解）仍在后台跑，落地后会再整形并与本表合流取优。
