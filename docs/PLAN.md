# 复赛下一步：先可行，再优化

这是待实施方案，不是完成记录。更新 2026-09-26 晚。**第 1、2 条已完成；第 3 条有原型。**

1. ~~**修复放行依据**：补 v4 原始评分回执、阶段和 ZIP 哈希~~ —— **已完成**：`artifacts/rejected/submission_semi_merged_v4/official_feedback.json`，与 v2 那份同格式，另带附件回读哈希与定位串。
   **未完成**：副本的漂移自检仍只比较违规总数，未做完整内容比较；`audit-packages` 值得改。
2. ~~**独立验证条款 6**~~ —— **已完成**：`src/platform_check.py` 与 `src/platform_score.py` 均换成候选判据，`tests/test_clause6_anchors.py` 对四个官方回执（7030 / 7559 / 7033 / 7342）断言**两个实现**都精确复现，外加官方示例必须为 0。两份实现保持独立，由锚点测试而非手工同步来防漂移。
3. **改变生成结构** —— **进行中**：`tools/analysis/chain_layout.py` 把方案排成"一个交界最多一个跨轮订单"的形状，在 v4 上布局成功 2,024 / 3,200，接缝 7,344 → 3,464，质量类错误全清零。**尚不可提交**；失败原因、一个必须先定的形状问题、以及三种失败尝试都记在该文件的模块文档里。**先量过 2,024 再动手。**
4. **生成全量可行基线**：联合搜索并排根数、整数段数、坯型和坯数，重新检查所有 12 条约束；不允许通过欠产、删除订单或跨方案拆单换取连续性。不得把机械拆轮当完整修复，尤其要检查 50m 下限与最多 6 轮。
5. **验证提交**：明确授权后只提交可追溯 ZIP；从该次结果查询记录下载附件核对 SHA，再比较约束数与小分。回读失败标记未知，不能自动反复重交。
6. **再优化与计分校准**：获得有效分后区分浮点取整、权重和子分舍入；用已验证结构做局部搜索/多种子。旧岛只作素材，重新通过约束后才复用。
7. **交付复现**：在 Linux 干净环境复现最终结果，形成真实的代码、数据审计和 ≥3000 字技术报告；按 [RULES](RULES.md) 的两个截止时间交付。

## MCP 的后续改进

使用独立仓库 [new_mcp](https://github.com/WRw5w/new_mcp)，以其当前代码和 README 为准。
登录门与等数据行的改动方向正确，但吞掉读取异常仍可能把故障说成“无记录”。
需区分未登录、加载失败、无匹配记录、处理中、已评分和附件不匹配，并补翻页/按候选哈希归因。
本整理不执行这些代码改动；后续监控遵守 task_watcher 和默认 30 分钟心跳约定。
