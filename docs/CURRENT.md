# 复赛当前状态

更新：2026-09-26。求解代码基线 `39957819cee78ab43355d053140778a4785a53ea`；文档整理不代表代码已修复。

## 已确认与待确认

- **规则**：50–150 m、宽 2 m、承重 60 t、每方案最多 6 轮；完整约束见 [RULES](RULES.md)。
- **平台反馈记录**：原始 v2 被判 7030 条跨轮接续违规、0 分；v4 新报告记载 7342 条、0 分。v4 的原始平台回执尚未在该候选目录归档，本次未重新登录核实。
- **代码缺陷仍在**：`src/platform_check.py`、`src/platform_score.py` 仍用旧的交界首尾判据；`src/build_submission.py` 仍旋转键顺序。它们给出的 0 违规不能用于放行。
- **候选解释**：逐个检查共享订单、按交界去重，独立复算同时得到原始 v2=7030、v4=7342；已旋转 v2=7028。见 [证据](EVIDENCE.md)。这是候选判据，未取得官方评分器源码。
- **撤回的结论**：仅旋转键顺序即可合规；排序或 `orders` 列表顺序已被证明是平台规则；初赛拟合可以证明复赛权重。
- **搜索产物**：远端 `d597dd0` 记载迁移档案中的八个岛已完成，合并产出 v4。旧记忆里的三个未完岛不能直接当重跑依据；新机器先核对实际 chunks。
- **预测成绩**：v4 的 95.087 是旧模型假设下的本地预测，平台反馈记录为 0；不是有效成绩或可提交推荐。

## 现有包状态

`submission_semi_FIXED`、`submission_semi_merged_v2`、`submission_semi_merged_v4`、
`submission_semi_nolimit`、`submission_semi_safety`、`submission_ours_94`、`submission_ours_seed`
以上目录统一位于 `artifacts/rejected/`，均作为历史候选/回归样本保留，不能直接提交。`FIXED`、`safety` 是历史目录名，不是质量认证。
七包 ZIP 与旁边 JSON 的解析内容和键顺序已核对一致；部分空白序列化字节不同。
`audit-clause6` 已改为候选判据复算（7030 / 7342 / 官方示例 0），输出明确标记未认证；生产校验器尚未更换。
七包现行 `validation_report.json` 均为 `status=unverified`、`passed=false`、`submission_allowed=false`；旧报告逐字节保留为 `validation_report_legacy_B.json`。`resync` 也不会把旧模型通过升级成认证。
`audit-packages` 的漂移自检仍只比较 B 违规数量，无法证明任意两份 JSON 一致。

## 优先次序

1. 固化原包哈希与平台回执，修复误放行的验证和报告；详见 [方案](PLAN.md)。
2. 生成每个订单只占一个连续生产区段的全量可行基线。
3. 经明确授权进行一次附件哈希可归因的验证提交，取得有效分和小分。
4. 再校准复赛计刀/权重并优化，不继续盲跑旧违规结构。

结果提交截止 **2026-10-05 20:00 +08:00**；代码截止 **2026-10-07 23:59 +08:00**。
每天最多 5 次，取复赛阶段最高分；榜单存在延迟。来源见 [规则](RULES.md)。
