# 复赛当前状态

更新：2026-09-26 晚。求解代码基线 `e149d07`。**已有一个通过全部本地校验、零违规的候选包**；尚未提交。

## 已确认与待确认

- **规则**：50–150 m、宽 2 m、承重 60 t、每方案最多 6 轮；完整约束见 [RULES](RULES.md)。
- **平台反馈记录**：原始 v2 被判 7030 条跨轮接续违规、0 分；v4 被判 7342 条、0 分。两份回执均已按同一格式归档到各自候选目录的 `official_feedback.json`；v4 那份带平台回读的附件哈希，与提交的 ZIP 逐字节一致（attachmentMatches=true）。
- **校验器已修**：`src/platform_check.py` 与 `src/platform_score.py` 都换成了候选判据，四个官方锚点（7030 / 7559 / 7033 / 7342）在**两个实现上**都精确复现，另有官方 PDF 示例必须为 0。回归见 `tests/test_clause6_anchors.py`。
- **候选判据**：交界处**每一个**共享订单都必须同时在左轮末尾和右轮开头；两个以上共享订单的交界必然违规。按交界计一次。仍未取得官方评分器源码，故报告一律标记未认证。
- **死路**：仅旋转键顺序即可合规（平台不看字典键插入顺序，`_v4` 就是这样得 0 分的）；排序或 `orders` 列表顺序（官方示例会被它判违规）。
- **搜索产物**：迁移档案里的八个岛已完成，合并产出 v4。旧记忆里的三个未完岛不能直接当重跑依据。

## ✅ 合规候选（2026-09-26 晚，首次）

`artifacts/candidates/cand_compliant/`（ZIP SHA-256 `2c212615…0f8392`）与 `cand_rounded/`（`46c7fd25…ad51da`）。

在**不重解**的前提下把 v4 的 3,200 个方案重新排成"一个交界最多一个跨轮订单"的形状：

| | v4（违规） | 合规候选 | 四舍五入版 |
|---|---:|---:|---:|
| 平台校验 | 7342 条接缝 | **全部通过** | **全部通过** |
| 订单 | 9,999 | 9,999 | 9,999 |
| 覆盖率 | 99.97% | 97.42% | 97.42% |
| 刀数（floor 口径） | 167,748 | 180,299 | **177,226** |
| 预测分（floor） | 95.09（假） | 91.40 | **92.01** |

构造与四个坑见 `tools/analysis/chain_layout.py` 与 `build_compliant_candidate.py` 的模块文档。要点：**质量下限用的是原始直径的 linear**（`physical_linear_weight`），不是 `linear_weight`（取整直径）——两者在 9,999 单里有 2,465 单不同，用错会让必需 count 超出床宽上限，739 个方案因此被拒。

## ⚠️ 未决：刀数取整口径，值 1.6 分

`cand_compliant` 与 `cand_rounded` 是**同一方案**，只差长度的小数写法：

| | floor 口径 | snap 口径 |
|---|---:|---:|
| `cand_compliant`（浮点乘积） | 180,299 | 185,229 |
| `cand_rounded`（四舍五入成小数） | 177,226 | 185,229 |

两者在 snap 下**完全相同**、在 floor 下差 3,073 刀。**同一天提交这两个，看榜面回分的差就能定案**：有差 ⇒ floor，无差 ⇒ snap。这也同时验证候选判据（两次回执只报跨轮接续，说明其余约束都过了）。

## 现有包状态

七个旧候选位于 `artifacts/rejected/`，作为历史与回归样本，不能直接提交。现行 `validation_report.json` 均为 `status=unverified` 并带现行判据的计数（6625 / 6350 / 6625 / 7028 / 7342 / 7020 / 7559）；旧报告逐字节保留为 `validation_report_legacy_B.json`。

## 优先次序

1. 提交上面两个候选，用分数差定刀数口径。
2. 依结果决定是否重解（提高覆盖率：合规版 97.42% 对 v4 的 99.97%）。
3. 经证实的分数之后，再优化。

结果提交截止 **2026-10-05 20:00 +08:00**；代码截止 **2026-10-07 23:59 +08:00**。每天最多 5 次，取复赛阶段最高分。
