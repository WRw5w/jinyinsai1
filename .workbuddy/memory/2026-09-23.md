# 2026-09-23 工作日志（jinyinsai1 · 复赛）

承接 2026-09-22（clause 6 根因反推 + 合规化重造定形器）。本轮把**合规版交付包从 r=0.10
切到 r=0.25**、补齐报告口径、跑完四道闸门，并**推送成功**。

## 1. 交付状态（已定稿并推送）

- **commit `371f209`**（"复赛重交版：clause 6 合规化 + r=0.25，四道闸门全过"），
  21 files / +3370 −576；`7acd1bd..371f209 main -> main`，走 **`ssh-origin`（git@github.com:22）**。
  远端 HEAD 已用 **api.github.com** 逐文件 blob sha 比对：7/7 全部一致（`PARITY: ALL MATCH`）。
- **交付指标**：1,855 方案 / 9,914 轮 / **176,563 刀**（nearest 读法 185,577）/
  成材率 **89.80%** / 覆盖率 **100.00%** / `violation_count = 0` /
  总分 **93.187（B=160k）** 或 **77.329（B=90k）**。
- **哈希**：`json_sha256=cd0d2036e04ec0e7c08ec225e25621fb66d07a9fb51aef45f2da963c2eec8de1`、
  `zip_sha256=649ed7ddc9377bdd9d87978b4be8891c320ac10e4dfb4b7d244aabdd3022bf79`、
  代码包 `6b39e13dc4c652c3f5d59848438bdfdf668493d7499455c8fa2419c9043fd90c`（65 条目）。
- `verify_deliverables.py` → **ACCEPTANCE: PASS**（4 道闸门）。

## 2. 本轮最关键的一条新证据：全卷确定性

两次**完全独立**的 16 进程全卷构建（`runs/cont_deliver25` 与 `runs/cont_dump25`，
各自跑完 12 点 λ 扫描 + 四道闸门）产出 `result.json` **逐字节相同**：
892,512 B / sha256 `d19360c548c3b01b4cd8d5a5ce7ee8051d7f2053c93d8a6236768838b85f73b6`，
且两次都选中 λ=4800 / 176,563 刀 / 93.187。→ 这条把"确定性"从**单键回归**升到**全卷**，
可直接回答评审"你的结果能不能复现"。

## 3. §6.7 的诚实化改造（本轮新增 §6.7.1）

前稿 §6.7 只有 **旧定形族**（round_shaper + `build_semi_plan.pair_group`）的 λ×B 表，
交付点 λ=2400，与交付族（continuity_shaper + r=0.25，λ=4800）**不是同一族**，属硬伤。
本轮补上交付族的表：

- **做法**：`build_semi_plan2.py --dump-candidates runs/cand25.json` 把每组的
  `(knives, declared, batch)` 帕累托集落盘 → 新脚本
  `diagnostics/semi_lam_delivery_family.py` 只重选候选，**12 点 λ × 2 刀口径 × 3 基准
  只需 2 秒**（不必重跑 7 分钟装桶定形）。这是"候选表落盘"设计的最大回报。
- **结果**：交付点 λ=4800 在 **(floor, B=160k)** 上恰是**内部最优**（gap 0）；其余五格
  落后 0.011 ~ 0.257 分。机制复现：B 升到 180k 后刀项在 λ≥1200 即封顶（`180000/179186>1`），
  目标退回纯成材率，最优 λ 左移到 1200/3200。
- **额外做了 minimax 遗憾矩阵**（写进报告 §6.7.1）：最坏遗憾最小的点是 λ=1600（0.129），
  交付点 λ=4800 排第 4（**0.257**）。**仍保留 4800**，理由写进报告：
  ① 最可信读法下的最优点；② 与复现命令 `--sweep` 的 `CHOSEN` 输出一致，手工改投会切断
  可复现性；③ 0.13 分的差距比刀数取整口径的风险（0.99 分）小一个数量级。

## 4. 本轮修掉的两处真实口径不一致

1. **打包报告的基准刀数**：`build_submission.py` 曾漏传 `baseline_knives`，
   于是报告按 `platform_score` 的默认 90,000 计分（76.617），而构建驱动用 160,000。
   已加显式 `--baseline-knives`（semi 默认 160000），并在 `提交说明.md` /
   `validation_report.json` 里**同时给出 90000 与 160000 两个读数**，不取其一。
2. **§6.2 的"违规恒为 0"是错的**：`r = 0.02 / 0.05` 两行实测 `violation_count = 453 / 24`
   （窗口太窄装不下全部订单）。已在报告里标注这两行**不是可交付方案**，并说明
   分数列**未计入违规罚分**；§7-4 的"备选 r=0.02"也随之改成**最小的干净备选 r=0.10**。
3. 附带修掉：覆盖率那条（前稿说 1 单没赚到覆盖率停在 99.99%）**本轮已解决** ——
   新装桶把 `B20275738` 并入方案 #841（3 单/3 轮），交付包中 **solo 方案 = 0**，
   对有效订单覆盖率回到 100.00%。

## 5. 工具/环境（本轮新增两条）

- **临时文件别再写仓库根**：sandbox 的 `Remove-Item` 仍被 `SAFE_DELETE_FAIL_CLOSED` 拦，
  删不掉。改成 `Move-Item` 到 `runs/_scratch/`（`runs/` 已 gitignore）→ 仓库根干净。
  另外把打包测试的残留目录 `submission_semi_test/` 写进 `.gitignore`。
- **`git push` 的输出走 stderr**，PowerShell 会渲染成 `NativeCommandError` 红字，
  但 `*>&1 | Out-File` 里能看到真正的 `7acd1bd..371f209 main -> main`——**别误判成失败**。
- 交付族的 λ 敏感性脚本用 `runs/cand25.json`，**先跑 dump 再跑诊断**，顺序错会拿不到候选表。

## 6. 待办 / 未决（口径一条都没被官方确认）

1. **刀数取整口径**（`int(L//size)` vs `nearest`）：同一份方案差 **9,014 刀（5.1%）**，
   值 0.99（B=90k）~ 1.76 分（B=160k）。判断 `nearest` 更可能是平台口径
   （否则交付判定会崩），但无法自证。
2. **基准刀数 B**：160000 只见于群答疑归档（`evidence/rules/BASELINE_160000_SOURCE.md`），
   非官方书面文件。
3. 若官方另有隐性超产上限，最小的**干净**备选是 `r = 0.10`（77.329 → 76.617，B=90k）。
4. 口径一旦确定：`--load-candidates runs/cand25.json` 重跑 λ 网格即可换点，**秒级**，
   不必重建。
