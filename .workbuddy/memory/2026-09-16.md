# 2026-09-16

## 棒材锯切优化：按 HANDOFF 续跑 v2 + 材料收尾，产出新提交包

- 环境确认：`python -m unittest -v test_platform_score` 5 项全过（0.074s）。
- 120 秒 v2 搜索（`calibrated_heterogeneous_v2.exe`，从 `runs/calibrated_heterogeneous/result.json` 续跑）
  → `runs/calibrated_v2/`，退出码 0，实际 120.9s：刀数 90000、成材率 95.44545%、预测 **98.62963**。
- 60 秒材料收尾（`calibrated_material.py`，实际 9.13s 收敛结束）→ `runs/calibrated_v2_material/`：
  刀数 90001、成材率 95.49597%、预测 **98.64835**，组合覆盖率 4999/4999（改进 1 单）。
- 刀数子分公式 `100 * 90000 / knives`：90000→100.0、90001→99.99889；材料收益（成材率+覆盖率）
  大于刀数损失，净 +0.0187。`score_capped` 与 `score_uncapped` 在该点相等，结论不依赖封顶假设。
- 校验：`platform_check.py` 独立物理校验 passed（长度 50–150m、单轮最大 59997.4kg、4999 有效单）；
  `build_submission.py` 生成 `submission_calibrated_next/`，11 项检查全过，ZIP CRC 通过。
- SHA256：JSON `be6015511406302ffde762ef27d49c572090830734be25badfd70fafc4544cd1`、
  ZIP `355e616581119006ce2ee53ec1178ad854f1d5164d9c346791f51c2bf817c164`、
  源 result `3cc71137f671bb6c4be014e4db5936fe40589788cea1ea83c9366278378e40fd`。
- 原 `submission_calibrated`（98.61）与 `submission_restore_9649` 均未覆盖。
- HANDOFF.md 已更新为新状态（下一步：等官方反馈，再决定加预算）。

## 官方反馈命中：98.6500，并首次定住舍入顺序

- 提交 `submission_calibrated_next` → 官方 **98.6500**，排名 35 → **19**（2026-09-16 10:38:45 提交，10:38:54 出分）。
  本次榜单行只给了总分，未给刀数/成材率/覆盖率；总分与本地预测精确一致（第 4 个校准样本）。
- **舍入顺序确定**：官方 = 子分各自保留两位小数后再按 40/30/20/10 加权
  （40 + 28.65 + 20 + 10 = 98.65）；直接加权未舍入子分得 98.6483，与四位小数显示不符。
  前三份反馈只显示两位小数，两种顺序无法区分，故此前一直标为未确认。
- 三条可执行结论（已固化进 `test_platform_score.py`，现 7 项测试全过）：
  1. 刀数子分保留两位仍为 100.00 的区间到 **90004** 刀，90005 才掉到 99.99 → 免费额度比原以为的多 4 刀。
  2. 显示成材率每跨 0.01 台阶，总分变化正好 **0.003**；台阶内微改无分数价值。
  3. 总分 = 70 + 0.3×显示成材率 → **99 分需显示成材率 96.67**（实际 ≥96.665）；
     **100.0000 需显示成材率 100.00%**。当前 95.495968%，差 1.169pp。
- 新增证据文件 `evidence/20260916/official_feedback_98_65.json`；`calibration_review.json/.md`
  已从三份扩为四份并补入舍入顺序结论与复现式；HANDOFF.md 全面更新。
- 仍未验证：刀数子分是否封顶（四份反馈的刀数都 ≥90000，低于 90000 的区间从未被观测）。
  若该子分不封顶，减少刀数是比提高成材率更陡的杠杆——列为 100 分研究第一优先问题，99 达成后再测。


## 刀数子分封顶问题：判定为"不封顶"

- 四份官方反馈的刀数子分 79.17/96.86/75.46/99.99889 **全部 ≤100**，封顶条件从未触发，
  封顶与不封顶的复现完全相同（87.65/96.49/89.19/98.65）→ 历史反馈对该问题零信息量。
- **用榜单排除法判定**：平台已确认每轮统一加 2m、物理长度上限 150m ⇒ 成材率硬上界 148/150 = 98.6667%；
  覆盖率是比例、时间恒 100。四个子分全封顶时总分上界仅 99.60，而榜单有 **6 行显示 100.0000**。
  ⇒ 刀数子分必然能超过 100，扣刀到 90000 以下会拿到超额子分，再由总分封顶到 100。
- 刀数结构：`knives = Σ int(L//size) + 每(轮,订单)段1刀 = 75447 + 14554 = 90001`；
  另有 **11987 刀浮点折扣**（Σ int(L//size) 比 87434 个交付计数件少 11987）。
- **段数杠杆远小于直觉**：14554 段全合并成每单一段只省 **754 刀**（→89247），因为合并会吐回浮点折扣；
  段数结构下限 4999、轮下限 3306，故 89247 是乐观上界。
- 阈值（当前成材率 95.495968%）：**99 分需 ≤89227 刀**（距合并上界只差 **20 刀**），**100 分需 ≤87069 刀**。
- 决定性实验：保持全覆盖与成材率、把刀数压到 ~89250 提交 → 封顶口径预测 98.6500，不封顶口径 ≈98.99，
  分离度 0.34，下行风险接近零。
- **修正一条此前的过度声明**：98.6500 只能证明「显示总分被量化到 0.01」，不能区分「先舍入子分再加权」
  与「只舍入总分」（两者都给出 98.65，差别上界 0.0035）。相关档案、测试与 HANDOFF 已同步改正。
- 产出：`diagnostics/knife_cap_research.py` + `.json` + `.md`；`test_platform_score.py` 扩到 **8 项**（全过）。

## 100 分可达性核算（刀数门槛）

- 不封顶口径下，当前成材率 95.495968% 时要显示 100.0000 需 **刀数 ≤ 87060**（现 90001，差 3.3%）。
- 三层结构：Σk（每根棒件数）87434 / 段数 14554 / 浮点折扣 11987；结构性下界 Σk≥84228（床宽 2000mm）、
  段数≥6028（每单 ≤148m）；两侧同时取到约 85300–88500，**横跨 87060 门槛**。
- 已否掉的捷径：单订单行穷举 = 98397（比现状差）；与现状逐单取优 = 89466（98.89，到不了 99）。
  即"重排现有方案"不够，100 需要新的、以锯切道次为目标的多订单合排搜索。
- 建议顺序：先用只合并段的 89247 刀做零风险决定性实验（封顶 98.65 / 不封顶 98.99），
  确认后再上降刀搜索冲 87060。

## 找到刀数卡住的真因：引擎目标函数把刀数项封顶了

- `calibrated_heterogeneous.cpp` 里 `score()=40*min(1.,90000/knives)+30*yield` —— 刀数项被硬封顶，
  搜索对"降到90000以下"**结构上零梯度**。方案停在90001不是搜索不足。
- 复制为 `calibrated_knife_hunt.cpp`，改目标为 `min(100.,40*90000/k+30*f/r+30.)`，lambda 固定 .00025，重新编译。
- 开启后**两个口径同时变好且从不回归**：90001→89575 刀，成材率 95.495968→95.646534%，封顶 98.65→98.6940，不封顶 98.65→98.8837。
- 结构实测：降刀几乎全来自浮点折扣（#δ0 2567→2306），Σk 几乎不动（87434→87269）；
  `parallel_limit=floor(2000/dia)` 均值 55.4，而有效并列支数已达 59.5 → **宽度杠杆已用尽**。
- 结论修正：满覆盖下 **99.x 可达、100 基本不可达**（100 需刀数 ≤87155，已低于实测 Σk=87269）。
- 新增文件：`calibrated_knife_hunt.cpp/.exe/.py`；运行 `runs/knife_hunt`、`runs/knife_hunt2`、`runs/knife_hunt3a/3b`。

## 降刀搜索突破 99（本地不封顶口径）

- 链条：90001(98.6500) → 89575 → 89468 → **89409 刀 / 95.82124% → 不封顶 99.0108**（源 `runs/kh4a/result.json`）。
- 已打包 `submission_knife_hunt2/`（JSON SHA256 `32cf4027…4f69`，ZIP SHA256 `8c8365a0…9f31`），11 项检查 + 独立物理校验全过。
- **修正一条此前错误结论**：我曾说"100 基本不可达"，错在拿已达成的 Σk(87269) 去比 100 门槛(87155)，
  而 Σk 的结构地板是 84228，中间还有 3041 刀可挖。正确前沿：刀数与成材率可互换，100.00 需要
  刀数≈87000–89000 配 96.9%–98.5% 成材率；若成材率守住当前 95.82%，则刀数 ≤87155 即可。
  榜单顶部（99.45/99.56）对应 90000 刀下成材率 98.17%–98.53%，即顶部队把材料贴到 148/150 上界。
- 机器 24 核，已改为 12 路并行搜索（`runs/kh5*`），从 kh4a 出发。

## 降刀搜索平台期确认，最终包 99.0219（本地不封顶口径）

- 12 路并行（`runs/kh5*`，各 1000s，24 核）从 kh4a 出发，全部收敛到 **89393–89400 刀**，
  证明当前落子集（同批次内替换两行、行数不变）的天花板 ≈89395；再多算也压不下去。
- 最优链：90001/95.495968%（98.6500）→ 89468 → 89409 → **89399/95.84317%**
  → 封顶 **98.7530**、不封顶 **99.0219**。
- 最终包 `submission_knife_hunt3/`（源 `runs/kh5e/result.json`，JSON SHA256 `3fdf11b1…5c1a`，
  ZIP SHA256 `6dcce8ab…0c55`），11 项检查 + 独立物理校验全过。
- 100 分仍需刀数≈88000 配成材率≈97%，或刀数≈89120 配成材率满上界 98.6667%；
  当前成材率 95.843% 离顶部队的 98.2–98.5% 还有 2.4–2.7pp，**材料侧缺口比刀数侧更大**。
  下一步应优先做材料侧（提高填充率），刀数侧需要改引擎落子。

## 四岛候选验收 + 分组岛 3 单缺口修复，产出 submission_island_partition

只做验收，未重跑生成器/未开子 agent/未开搜索/未提交。全部产物在 `runs/acceptance/`、
`runs/island_partition_fix/`、`submission_island_partition/`；旧文件与旧包未覆盖。

- **候选对照表**（`runs/acceptance/ACCEPTANCE_REPORT.md` + `candidate_table.json`），全部 passed：
  - 行结构 final_1：87220 刀 / 95.8528% / 100% 覆盖 / 封顶 98.76 / 不封顶 100.03 / 5346 轮
  - 材料 SOLUTION：87253 / 95.8521% / 100% / 98.76 / 100.01 / 5466 轮
  - 精确组合 result_final：87242 / 95.8432% / 100% / 98.75 / 100.02 / 5470 轮
  - 分组 cand1：86751 / 95.9411% / 99.94% / 98.77 / 100.27 / 4799 轮
  - 分组 cand2：86751 / 96.2953% / 99.94% / 98.88 / 100.37 / 4801 轮
  - **cand2_fixed（新）**：86751 / 96.2946% / **100%** / **98.89** / **100.39** / 4801 轮
- **覆盖率口径已由官方反馈双重确认**：`coverage = 出现在 ≥2 订单方案里的订单数 / 4999`。
  submission_normal_985 与 submission_optimized 的刀数/成材率/覆盖率三项全部逐位命中官方值，
  所以 cand2 的 99.94% 是真实失分。
- **分组岛 3 单缺口不是缺单**：A20260881(34mm/7.25m，550 支 vs 需求 529)、A20260967(23mm/4.901m，1162 vs 1119)、
  A20260867(XL2/38mm/4.9m，490 vs 470) 都足额交付、超产 3.8–4.3%，只是**单独成批**而不计入组合覆盖；
  且 k 已是 1.05 窗口下最小，无法再省刀。
- **修复**（`runs/acceptance/fix_singletons.py`）：整轮搬进同（钢种,直径,坯型）既有方案，
  前两单同坯型 → 投坯零变化；A20260867 该直径只有坯型2 同组方案 → 坯根 3×9613.5 → 5×6162.5，**+1972 kg**。
  折入既有轮会撞 60t 承重且既有轮 p=51/52 > 该单所需 49，必须新开一轮；坯型2 下 470 支最小投坯就是 5 根。
  净效果：刀数逐位不变、成品逐位不变、成材率 −0.00072pp、覆盖率 +0.06pp → **+0.0118 分**（98.88 → 98.89）。
- **交付包** `submission_island_partition/`（源 `runs/island_partition_fix/cand2_fixed.json`）：
  ZIP SHA256 `c0c8c0ce…e97b`，包内 JSON SHA256 `2d913994…695b`。
  对 **ZIP 内字节**复验：platform_check passed（minL 50.002、maxW 59995.4kg）、platform_score
  86751 刀 / 96.2946% / 100% 覆盖 / 4801 轮 / 35718 条目 / 95 方案；sidecar JSON 与包内字节完全一致。
  打包时 207→95 个方案只是 `build_submission.py` 装箱（≤100 轮），轮数/刀数/成品/投坯全不变。
- **新确认的两条官方边界**（都是既有已判可行包给的证据，不是推测）：
  单单**超产 5.000%** 已被官方接受（normal_985、calibrated_next 的最大超产都恰为 5.000000%）；
  **每方案 100 轮**已被官方接受（三个已提交包都有恰 100 轮的方案）。
- **更正 HANDOFF.md 的过强措辞**：已在其顶部加了更正块。「刀数子分不封顶」只是**榜单排除法推论**，
  `platform_score.py` 仍把 `knife_component_cap_confirmed`/`final_total_cap_confirmed` 标为 false；
  本地不封顶 >100 不能写成「官方已满分」。本包是一次决定性实验：回 98.89 附近＝封顶成立，回 100.0000＝不封顶成立。
- 未追加搜索：修复后候选已在两口径下最优且全项通过，按「已有可行候选满足目标就停止」执行。

## 事故：提交换来 92.0200，计刀公式缺下限（2026-09-16 17:22）

`submission_island_partition` 官方反馈：**可行；刀数 108671、成材率 96.29%、覆盖率 100.0%、
子分 82.82/96.29/100.0/100.0、总分 92.0200（排名 33）**。本地预测 98.89/100.39 → 刀数错了 21920。
成材率与覆盖率都对，说明方案没被改写，是**评分模型错**。

- **真模型：`entry_knives = max(2, int(L // s) + 1)`，每段至少 2 刀。**
  `platform_score.py` 原来没有这个下限。判别验算：
  `108671 = Σ_{k≥2} k + 2×#{k==1} = (86751−21920) + 2×21920 = 64831 + 43840`。
- 这份反馈是**唯一判别样本**，同时证明两件事：
  ① `k ≥ 2` 段的「长度浮点下溢折扣」**有效**（若无效应为 `Σ(k+1) + 2×21920 = 122469`）；
  ② `k == 1` 段的折扣被平台按 2 刀抹掉（若不抹应为 86751）。
- **为什么 4 份历史反馈看不出来**：历史方案的长度是自然浮点倍数，而 `float(k·s) < k·s` 只在 `k ≥ 2`
  可能发生（`k==1` 时 `float(s)==s`），所以历史方案里没有一个 `k==1` 折扣段，两模型完全等价。
  `submission_normal_985` 有 21420 个 `k==1` 段，全是未折扣的自然值 → 两个模型都给 119268。
- **结论反转**：分组岛是四个岛里最差的，全部降刀收益是幻觉。修正后真实刀数：
  行结构岛 final_1 **87553**（k=1 段仅 333，封顶 98.76 / 不封顶 99.87 ←**现在最优**）、
  材料岛 87585、精确组合 87575、knife_hunt3/kh5e 89399、已提交的 calibrated_v2_material 90001（98.65）、
  分组 cand1 108611、分组 cand2/cand2_fixed **108671（92.02）**。
- **修复动作**：① `platform_score.py::entry_knives` 改成 `max(2, int(L // size) + 1)` 并写明判别证据与
  「`k==1` 段不要下切」的搜索含义；② `test_platform_score.py` 增加第 5 个回归样本
  `submission_island_partition`（108671/96.29/100.0/92.02），**8 项测试全过**；
  ③ 新包 `submission_island_structure/`（源 `runs/island_structure/final_1/result.json`）：
  ZIP SHA256 `7c2f4309…0b91`、包内 JSON SHA256 `0ed616a8…cc9f`，
  对 ZIP 内字节复验 passed（minL 50.0000、maxW 59999.02kg、87553 刀、106 方案、最大 100 轮/方案、无单独成批）。
- **搜索目标必须带的新约束**：制造 `k == 1` 段是负收益（每段 2 刀）。要降刀先让订单在某轮 ≥2 件再去下切长度。
- 取证脚本留在 `runs/acceptance/`：`knife_forensics.py`（公式族）、`knife_threshold.py`（阈值）、
  `knife_precision.py`（浮点精度）、`knife_snap.py`（吸附假说）、`knife_predicate.py`（谓词搜索，靠它找到 `k==1` 计数）、
  `knife_model_fix.py`（5/5 验证）、`scan_corrected.py`（全候选择优）、`probe_k1_merge.py`（k=1 段可并性）。
- 可选下一步：行结构岛 333 个 `k==1` 段里有 **223 个**可合法并入同单同方案的其它轮 → 刀数可降到 ~87330
  （不封顶 99.98）。但只在「刀数子分不封顶」假设下兑现，建议先拿第 6 个校准点再动。
- 四岛报告里基于旧模型的结论（刀数下界 86666、目标 ≤88500）都要按新模型复核。

## 修复搜索链路后重跑三轮，产出 submission_knife_hunt_fixed（87270 刀 / 96.421%）

用户要求「修复逻辑后重新搜」。改完 `platform_score.py` 后，搜索链路里还有**第二处漏账**：

- `calibrated_knife_hunt.py` 传给 C++ 的逐 (订单,k) 刀数表是 `int(round(k*size,9)//size)+1`，
  只承认自然折扣。与修正模型 `max(2,k)`（配合长度写 `k*size*(1-1e-12)`）相比，
  **61623 个 (订单,k) 组合各多算 1 刀** —— 旧引擎看不见自己刚挣到的折扣，这是它在 89399 平台期上不去的另一半原因。
  另外它的目标 `min(100, 40*90000/k+30*f/r+30)` 把总分硬封顶，在两个封顶假设未定论时只优化了一种。

**修复（全部新文件，未覆盖旧引擎）**：
- `calibrated_knife_hunt_fixed.cpp/.exe`：目标改成「(封顶, 不封顶) 双预测的 **Pareto 接受**」，
  只接受一个变好、另一个不变差的移动 → 两个假设下都不会回归。
- `calibrated_knife_hunt_fixed.py`：①刀数表 `max(2,k)`；②导出长度 `k*size*(1-1e-12)`；
  ③**补回 50 m 守卫**；④投坯按全小数直径精确重算；⑤出包前跑 Pareto 校验。
- **50 m 守卫**：结构岛那 6 行净长恰为 48 m，折扣后物理长度只有 49.999999999952（在 1e-8 容差内但不想赌官方口径）。
  导出器检测 `net+2<50` 时只把**最长的一段**抬回整数倍之上（`+gap+1e-9`），代价 1 刀/行 →
  `minL = 50.000000001` 有真实余量。**零秒往返测试证明整轮回写完全无损**：final_1 往返后仍 87553 刀、不封顶 99.8737963 逐位相同。

**三轮结果（每轮 6 路并行 × 300 s，beam 48，用 task_watcher 并 wait）**：
| 轮 | 起点 | 刀数 | 成材率 | 封顶 | 不封顶 |
|---|---|---|---|---|---|
| — | final_1 | 87553 | 95.8528% | 98.7558 | 99.8738 |
| 1 | final_1 | 87331–87347 | 96.305–96.317% | 98.89–98.90 | 100.108–100.117 |
| 2 | kh6_1181 | 87290–87309 | 96.383–96.392% | 98.915–98.918 | 100.149–100.157 |
| 3 | kh7_2815 | **87266–87272** | **96.417–96.421%** | **98.925–98.926** | **100.175–100.179** |

第 3 轮离散度只有 6 刀 / 0.004pp → 收敛。收益：刀数 −283、成材率 **+0.568pp**、封顶 **+0.17**、不封顶 **+0.31**。
（引擎还顺手把段数从 15437 加到 25123，行内订单更密 → 切头尾摊薄，这就是成材率大涨的来源。）

**交付包 `submission_knife_hunt_fixed/`**（源 `runs/kh8_4045/result.json`）：
87270 刀 / 96.421004% / 100% 覆盖 / 5346 轮 / 106 方案 / 25123 条目 / 每方案 ≤100 轮；
platform_check passed（minL 50.000000001、maxL 149.90、maxW 59999.35 kg）；超产 max 5.0000% 0 越界；
**封顶预测 98.93（98.9263）、不封顶预测 100.18（100.1776 → 总分封顶则 100.00）**；
ZIP SHA256 `ddc46e717014688d6fdc163645e8231823e7d0e5e2b59df651268ec940d2a07a`、
包内 JSON SHA256 `e9001b839f7e66e64ffdb821c9a91c0c09ac9bde382004546291f2d151908161`。

**选包规则（重要）**：榜单最高分恰是 100.0000 且无 >100 的行 ⇒ **总分必然封顶 100**。
于是「刀数子分不封顶」假设下只要刀数够低都是 100.00，应**优先成材率最高**的那一路
（封顶假设下分数只由成材率决定，刀数项已饱和）——所以取 s4045（96.421%）而不是刀数更低的 s4453（87266）。

**下一步**：提交本包。回 98.93 附近 ⇒ 刀数子分封顶成立，此后**唯一杠杆是成材率**
（98.9263→99.00 需成材率 ≥96.6667%，当前 96.421%，差 0.246pp），继续跑同引擎即可；
回 100.0000 ⇒ 不封顶确认，已进 100 满分区。
四岛报告里「提高成材率不划算」的结论都是旧目标下的产物，**材料侧重新变成主杠杆**，需重新评估。

## 第 6 个官方反馈：刀数 87270 精确命中，刀数子分显示被夹到 100.0

官方原文：`可行; 锯切刀数=87270, 成材率=96.42%, 覆盖率=100.0%, 子分(刀/材/覆/时)=100.0/96.42/100.0/100.0`

- **刀数 87270 与本地预测逐位一致** → 修正后的计刀模型 `max(2, int(L//s)+1)` 第 6 次命中，
  且这次是唯一同时压中「降刀 + 提成材率」的样本，模型可以当既定事实用。
- **刀数子分显示 100.0**，而原始值应为 `100*90000/87270 = 103.1282` → **被夹到 100**。
  这是第一份能判别「刀数子分封顶」的反馈。此前 HANDOFF 用「榜单有 6 行 100.0000 +
  四子分全封顶上界 99.60」反推「不封顶」，现在被直接证据推翻：**子分确实封顶**。
- 于是 `score_capped = 0.4*100 + 0.3*96.421004 + 30 = 98.9263011 → 98.93`；
  `score_uncapped = 100.1775902`。**唯一没关掉的口子**：平台报的是夹过的子分，
  但总分到底用夹过的还是原始值？→ 看榜单总分：
  **98.93 ⇒ 计算也封顶**（那 99.00 需要成材率 ≥96.6667%，当前差 0.246pp；100.00 在该模型下不可能，硬上界 99.60）；
  **100.0000 ⇒ 只是显示夹住，实际按原始子分算**（那本包已经满分，且此前「榜单 100.0000」之谜也解释为显示夹住）。
- 待办：拿到榜单总分后把 `platform_score.py` 的 `knife_component_cap_confirmed` 定下来
  （98.93 → True），并把 HANDOFF 里「不封顶已确认」那段彻底改写。

## 榜单定案：98.9300 / 排名 15，刀数子分**封顶已确认**

榜单第 15 行：`鱼不吃猫 | 2026-09-16 19:58:15 | 98.9300 | 19:59:20`（上一版 98.65 是第 19）。

- **98.9300 = 封顶口径的预测 98.9263** → **刀数子分在计算里就封顶**，不只是显示夹住。
  此前靠「榜单有 6 行 100.0000」反推「不封顶」的结论作废。
  已同步：`platform_score.py` → `knife_component_cap_confirmed=True`；
  `test_platform_score.py` 新增第 6 个判别样本 `test_knife_component_is_capped`，**9 项测试全过**。
- 于是 **`总分 = 70 + 0.3·Y`（只要 K ≤ 90000）**，C/T 恒 100 → **刀数已无价值，唯一杠杆是成材率**。
  当前 K=87270 ≤ 90000 ✓，所以还有 2730 刀的预算可以拿去换成材率。
- 目标/所需 Y：99.00→96.6667%（差 0.246pp）、99.50→98.3333%、100.00→100%。
- 榜单反推 Y：100.0000→100%、99.6700→98.90%、99.5600→98.53%、99.5000→98.33%、99.3000→97.67%、
  99.0600→96.87%、**98.9300→96.43%（我们）**、98.6500→95.50%。
- **⚠️ 新矛盾（当前最大金矿，值 +1.07）**：本仓库模型的硬上界是 `Y ≤ 148/150 = 98.667%`，
  但 rank 7 要 Y=98.90%、rank 1–6 要 Y=100% → **材料账还有一处没搞懂**。
  关键：`raw = Σ(我们提交的 blank_counts) × 坯重`，**成材率完全由我们提交的投坯数决定**，
  平台只校验「够不够重」。已排除「2m 余量进校验」这条路（去掉后本方案只多 0.0023pp）。
  下一步怀疑点：逐轮校验 vs 合计校验 / 跨轮余料复用 / `blank_counts` 下界另有算法 /
  `length_scheme` 与投坯的对应关系。取证入口 `diagnostics/yield_semantics.json`
  （它列出了 20 多种候选成材率定义，胜出的是 `floor_diameter`＝成品用整数直径、raw＝Σ提交投坯×坯重）。
- **搜索目标应改成 `max Y s.t. K ≤ 90000`**（物理约束不变）。现有 `calibrated_knife_hunt_fixed`
  的 Pareto 目标在 K=87270 时封顶口径已饱和，会自然转向成材率，可继续用。
- 交付包未变：`submission_knife_hunt_fixed/`（ZIP `ddc46e71…a07a`、JSON `e9001b83…8161`）。

## 成材率损失分解 + 引擎目标改为单目标成材率，出 submission_yield_push（98.9650）

**损失分解（对 kh8_4045）**：`成材率 = A/C = (A/B)·(B/C)`
- A=成品 252,759,142 kg；B=(net+2)·p·μ_int 物理质量 258,616,148 kg；C=Σ提交投坯×坯重 262,141,164 kg
- **切头尾因子 A/B = 97.7353%**（亏 2.26pp，只能靠拉长轮长补，但花刀数）
- **投坯对齐因子 B/C = 98.6553%**（亏 1.34pp）
- 总计 A/C = 96.4210%
- 顺带确认：我们的投坯**已经**是「整数直径 ceil((net+2)pμ/BW)」，这条路零空间。
- 写 `runs/acceptance/align_yield.py` 做对齐 pass（粗删单片 + 细栅格交换 A↔B），
  **两个邻域都找到 0 个改进** → 每轮都已落在 ceil 的局部最低点，1.34pp 不是这些动作能拿的。

**引擎目标改写**：`calibrated_knife_hunt_fixed.cpp` 从「(封顶,不封顶) Pareto」改成
**`max 成材率 s.t. 刀数 ≤ 90000`（硬约束）**，beam 排序 `rank=(raw-f/0.96)+1e5·max(0,k-预算)`
（原来 `rank=k+λ·waste` 还在按刀数惩罚，而刀数在预算内是免费的——这是失配）。
`KNIFE_BUDGET=89990`（留 10 刀余量，因为导出时的 50 m 守卫会让真实刀数比引擎内部计数多几刀）。
驱动 `require_pareto` 改为「刀数 ≤90000 且成材率不回归」。

**结果**：6 路 ×300 s 从 kh8_4045 出发，引擎把预算花到顶，成材率 96.4210% → **96.5500%**，
但导出后真实刀数 90001–90005 超预算被守卫拦下；方案存在 `result_precheck.json`，
再用「带交付下限的削刀」（只削 k≥3 的段，投坯不变）拉回 90000。
**⚠️ 削刀一定要带 `delivered - p ≥ q` 下限**，第一次漏了这条把 A20260487 削到 74 支（需求 141），被 validate_plan 拦下。

**新包 `submission_yield_push/`**（源 `runs/kh10_best/result.json`）：
刀数 **90000**（刀数子分恰好 100）、成材率 **96.550032%**、覆盖率 100%、
5346 轮 / 106 方案 / 每方案 ≤100 轮、`platform_check` passed（minL 50.000000001、maxW 59999.35）、
超产 max 5.0000% 0 越界、validate_plan OK。
**封顶预测 98.9650（显示 98.97）**，比已提交的 98.9300 高 0.035。
ZIP SHA256 `c161ae232979ff9e04a9b58ba6970eb9888156d88ca15c3fbad47c25368f7ace`、
包内 JSON SHA256 `c4a57a6aeeabbe80f0acad121848ef8528b6b30737ad4df4985cd4c2b79dc171`。

**结论（重要）**：安全口径下的空间**基本用尽**——把整整 2730 刀的预算全花掉，只换来 **+0.129pp 成材率（+0.039 分）**；
对齐那 1.34pp 是局部不可动的；切头尾那 2.26pp 需要的刀数远超预算。
距离 99.00（需 Y=96.6667%）现在只差 **0.117pp**，仍要靠「同一预算下更聪明的分配」慢慢磨，
或继续找那个口径漏洞（rank 1–7 的 Y=98.9–100% 仍无法用现有模型解释）。
