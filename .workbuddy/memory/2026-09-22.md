# 2026-09-22 工作日志

## 复赛三件套交付完成（09-22 上午）

起点：上一轮已跑通 `build_semi_plan.py` 全量求解（r=0.02，74.66 分），并写了技术报告草稿。
本轮把它推到**可交付状态**：定 r、定稿、打三个包、做独立验收。

### 1. 超产允量 r 的完整扫描 —— 曲线已饱和，定 r=0.10

补齐 r=0.25 / 0.5 / 1.0 三个点（每个 ~70 s）。**关键事实：分数对 r 弱单调递增，
但 r=0.10 之后边际收益用尽。**

| r | 刀数 | 成材率 | 实际超产(重量) | S(B=90k) | S(B=180k) | S(0.4/0.4/0.2/0) |
|---|---|---|---|---|---|---|
| 0.02 | 211,356 | 0.9211 | 0.89% | 74.659 | 91.692 | 73.870 |
| 0.05 | 178,735 | 0.9181 | 1.44% | 77.682 | 97.540 | 76.863 |
| **0.10 ← 交付** | **175,952** | **0.9181** | **1.67%** | **78.001** | **97.540** | **77.181** |
| 0.25 | 174,994 | 0.9172 | 1.82% | 78.085 | 97.513 | 77.256 |
| 0.50 | 174,785 | 0.9169 | 1.84% | 78.103 | 97.506 | 77.273 |
| 1.00 | 174,741 | 0.9169 | 1.85% | 78.108 | 97.506 | 77.277 |

**选 r=0.10 的理由（可复用）**：它在四种未决口径读法下**都不劣**——最差一格只差
0.25 分（横跨 78～99 分）。0.02→0.10 值 +3.34 分，0.10→1.00（名义允量×10）只值
+0.11 分。所以「把名义允量停在容易解释的整数上」几乎不付代价，是免费的稳健性。
**教训：当参数扫描曲线饱和时，应选饱和拐点而不是极值点，用「四种口径的最差格」做判据。**

### 2. 修了两个真问题

1. **`build_submission.py` 的《提交说明.md》模板是初赛写死的**（异常单写成
   `A20260949`、定尺 −1100 mm、还写着"七个官方校准点"）。打复赛包会输出错误信息。
   已改成按 `round_name` 分支（semi → `B20270281` / −25.418 t；prelim → 原文）。
   ⚠️ 第一次改漏了最后一段（`tail_note` 定义了却没用上），跑完看输出才发现
   —— **改模板后必须把生成的成品读一遍，不能只看"跑通了"**。
2. **`round_shaper.py` 的模块 docstring 与代码不符**：docstring 写
   `k_i = ceil(P_i/(n·p))`，代码（第 166 行）实为 `K_i = ceil(P_i/p)`，
   **件数与分几轮无关**。已按代码改正。

### 3. 交付物（`deliverables_semi/`，7 个文件）

| 文件 | 字节 | 说明 |
|---|---|---|
| `复赛结果_棒材优化.zip` | 122,000 | 根目录只含同名 JSON |
| `复赛结果_棒材优化.json` | 803,524 | 明文 |
| `复赛代码与模型.zip` | 420,861 | 55 项，**字节可复现** |
| `复赛技术报告.md` | 27,765 | 5,282 汉字 |
| `提交说明.md` | 2,898 | 自动生成 |
| `validation_report.json` | 7,558 | 三层复验 + SHA |
| `环境与复现说明.md` | 7,782 | 手写 |

SHA-256：zip `52A1B004…3E8FB52`、json `87B9812E…6E6E1FA4`、codezip `f398e925…51924f0e`。

### 4. 新增脚本

- `diagnostics/semi_hypothesis_table.py` → `.md`/`.json`：**报告 §6.2/§6.3 每个格子都由它生成**，
  禁止手算填表。诊断目录是 git 跟踪的，符合仓库既有约定。
- `tmp/make_code_zip.py`：固定时间戳 + 排序 = **字节可复现的 zip**（重打两次哈希不变）。
  这是评审能核对"拿到的是同一份代码"的前提。
- `tmp/verify_deliverables.py`：三件交付物的独立验收（19 项断言）。

### 5. 验收时发现的两个"看着像 bug、其实不是"的点（写进报告了）

1. **交付 JSON 里是 2,196 个方案，不是 5,000**：`build_submission.merge_compatible`
   把同 `(钢种,直径,坯型)` 的整方案装箱合并到每方案 ≤6 轮。它校验三条不变量
   （逐轮指纹相同 / knives·rounds·finished·blank_weight 不变 / 覆盖率不降）后才落盘，
   是纯重排。我最初把 `schemes == 5000` 写成断言 → 假失败。
2. **两个刀数**：`platform_score` 按 `int(L//size)` 计段（浮点折扣，39.8% 的组合上少 1）
   → 175,952；`validate_plan` 朴素计数 → 190,341。**不是矛盾，是口径**。必须照抄平台行为。

### 6. 可复用配方

- **"字节可复现 zip"**：`zipfile.ZipInfo(name, date_time=固定值)` + 按名排序 + `external_attr`，
  重打哈希不变；再写一个验收脚本把自己解到临时目录、**只用包内内容**重跑校验，
  证明包自足。这个套路对任何"要交代码给评审复现"的场景都适用。
- **验收脚本里不要写死业务常数**（我就因为写死 `schemes == 5000` 报了一次假失败），
  要断言**不变量**（轮数守恒、哈希自洽、重算分与交付分相等）。
- 报告里的数字一律由脚本产出并落盘到 `diagnostics/`，报告只引用。

### 7. 补：用时实测 + 时间子分口径的第三个读法（09-22 10:00）

**实测墙钟**（本机 Windows 11 / Python 3.13.12 / 16 核但全程单进程）：

| 步骤 | 实测 |
|---|---|
| `prepare_semi.py`（归一化 + 异常隔离） | 0.44 s |
| `build_semi_plan.py --sweep --cap-ratio 0.10`（含 9 点 λ + 三层校验） | **73 s** |
| `build_submission.py`（打包 + 重开存档复验） | 2.1 s |
| 合计 | **≈ 76 s** |
| `diagnostics/semi_hypothesis_table.py`（可选） | 2.6 s |

**确定性已被实测证明**：重跑一次产出的 `result.json` 与交付件**逐字节相同**
（SHA-256 均为 `87B9812E…`）；`prepare_semi.py --root <临时目录>` 产出的
`orders.normalized.csv` 也与 `data/semi/` 里的逐字节相同。

**两个"复现粒度"不同，必须分开说**（评审会踩）：
- `复赛代码与模型.zip` —— **字节可复现**（我用了固定 `date_time` + 按名排序）；
- `复赛结果_棒材优化.zip` —— 只有**内容**可复现。`build_submission.py` 用
  `archive.writestr(name, raw)`，`ZipInfo.date_time` 取写入时刻，所以整包哈希每次不同，
  但**包内 JSON 逐字节相同**。→ 复核口径必须写 `json_sha256`，不能写 zip 哈希。
  （若以后想让它也字节可复现，改 `build_submission.py` 传
  `zipfile.ZipInfo(name, date_time=固定值)` 即可。）

**时间子分口径其实有三个读法，不是两个**（README.md:165 记的）：
1. PDF **正文**：40/30/20/10，时间独立 10%；
2. PDF **同页公式**：`40×基准/刀数 + 40×成材率 + 20×覆盖率`，**根本没有时间项**；
3. 群答疑：时间并入成材率。
读法 2 与 3 在数值上等价（都是 (0.4,0.4,0.2,0)），已在报告 §6.3 第 4 行 priced。
时间项取 0 是最悲观下界（78.001 → 68.001，整表同减 10，不改变选点）。

**教训**：报告里写"用时/耗时"这类数字前，一定要**真测**——上一版报告我写的是
"26 秒"（那是 r=0.02 的配对耗时），交付口径 r=0.10 实际是 63–73 s，差近 3 倍。
参数一变，性能数字就变，"沿用上次的数"会写错。

### 8. 补：剩余空间分解 + 发现"主导一切的取整口径"（09-22 10:2x）

起因：用户问"**优化到极限了吗**"。写了 `diagnostics/semi_headroom.py`（新），并把
`diagnostics/semi_hypothesis_table.py` 扩了一个"取整口径"维度。报告新增 **§6.5 / §6.6**。

**结论一：算法意义上已到极限。** 三条独立证据：
1. λ 扫描 9 点里 2400 最优（解析最优）；
2. **配对窗口从 4 加倍到 8（`runs/pair8`），输出的 12,559 个回合逐条完全一致**
   （回合多重集 + 每单参与度全部相同）→ 配对启发式在其参数上是不动点；
3. `r` 放大 10 倍只换 +0.11 分。

**结论二：绝对意义上没到，但剩下的是一个口径问题。** 关键新发现：
**`solver._export` 写 `round(k*size, 9)`，浮点会落到整数下方 → 同一份方案两种读法：**
- `int(L//size)`（`row_metrics` 计费口径）：段 163,393，**总刀数 175,952**，S(B=90k)=**78.0006**
- `nearest`（`platform_check` 收件口径）：段 177,782，**总刀数 190,341**，S(B=90k)=**76.4539**
- 差 **14,389 刀 = 8.2%**，值 **1.55 分(B=90k) / 2.17 分(B=180k)**。

**这是全部未决事项里单项影响最大的**（> λ 余量 ±0.25 + r 余量 +0.11 之和）。
但**两种读法下 r 的排序不变**，r=0.10 仍与最优差 ≤0.08 分 → **选点稳健，绝对值不可自信引用**。
判断：`platform_check` 用 nearest 判交付是**唯一可能口径**（否则 39.8% 组合都判少交，
任何方案都爆违规）→ 平台对交付必须宽容 → 对计刀大概率也宽容 → **nearest 更可能是真实口径**。

**没有去利用这个折扣**：把每段写到整数倍下方 1e-15 可使分段计费归零（刀分子分封顶
100，B=90k 下凭空 +19.5 分），但那是利用评分器浮点表示而非解题，且一旦平台口径自洽
（对交付也 floor）会大面积"少交"。当前 8.09% 折扣只是 `round(…,9)` 的自然落点。

**成材率精确四分（残差 0.00，原料 545,633,120 kg）：**
| 去向 | 质量 | 占原料 |
|---|---:|---:|
| 计入分子（需求封顶） | 500,935,599 | 91.81% |
| 超产（λ 可调） | 8,508,433 | 1.56% |
| 共享修边 2m/轮（规则强制） | 13,351,098 | 2.45% |
| 未切尽（整根坯取整） | 22,837,991 | 4.19% |
**只有 1.56% 是选择；6.64% 结构性不可约。** 天花板 95.82%（整根不损料）/ 98.33%（+修边不损料）。

**刀数轴余量**：段数已低于物理下界 3.2%（`ΣP_i/plimit`=168,837 vs 实收 163,393，因 floor 折扣）；
轮数 12,559 vs 质量下界 8,810（余量 **42.6%**），但 11,046/12,559 轮被**重量**卡住、
净长中位仅占上限 0.68、占满>98% 的轮只 2.5%。λ 曲线证明：**再省 661 刀要付 0.31 个
成材率百分点，净分为负** → 余量"存在但已不值得取"。

**踩到的坑（写进脚本注释/报告了）**：
1. **质量基准必须分两套**：回合质量上限用**物理满直径** μ，成材率分子用**取整直径** μ
   （96.29/96.42 校准点）。混用会让分解对不上（我第一次把 `produced` 当成 `useful+trim`，
   结果差了一个 trim=13.35M kg）。
2. **`produced`(未封顶分子) 恰好 == `useful`(净长×p×μ_int)** → 说明方案 `length_scheme`
   **只声明净长，2m 修边不在其中**。所以正确分解是四项，不是三项。
3. 脚本里的质量数一律**从 `platform_score.evaluate` 取**，只做几何复算对拍，不做自己的
   "另一种算法"——否则报告口径会和评分器分叉。
4. `path.relative_to(ROOT)` 在传相对路径时抛 `ValueError`，要 try/except（我第一版
   跑 `runs/pair8/result.json` 时挂在这行）。

**收尾**：`diagnostics/*` 变了 → **代码包必须重打**。重打后 57 项 / 426,986 B /
SHA `d70fd7618cae3fd3334a13683cb3a807ea7cb6464a0c0c565fb2fd095807339e`（重打两次字节一致），
`tmp/verify_deliverables.py` 仍 **19/19 PASS**。报告 7,400+ 汉字 / 42 节 / 45 表。
另把 λ 扫描逐点证据存成 `diagnostics/semi_lam_sweep_r010.txt`（**新增证据文件**，
让"175,952 → 175,291"这条边际曲线可复核——之前它只存在于临时 stdout 里）。
**教训：报告引用的每个数字都要有一个入包的、可重跑的证据文件；只存在于临时日志里的
数字等于不可复核。**

### 9. 推 GitHub：证书缺一步，通道已打通（09-22 10:4x）

用户要求"试试能不能推上 git 仓库"。远端 `github.com/WRw5w/jinyinsai1`（**public**，默认分支
main，远端 5.1 MB）。结论：**网络不是障碍，缺的只是一份凭据。**

**按端口分测（关键，别再按域名下结论）**：
| 目标 | 结果 |
|---|---|
| `github.com:443`（git-over-HTTPS） | ❌ 超时；走沙箱代理 → `502 CONNECT tunnel failed` |
| **`github.com:22`（SSH）** | ✅ **通！** `ssh -T git@github.com` 回 `Permission denied (publickey)` |
| `ssh.github.com:443` / `:22` | ✅ 通 |
| `api.github.com:443` | ✅ 200 |
| `codeload.github.com:443`、`ghfast.top`、`gitclone.com` | ✅ 通（后两者只能拉，不能推） |

→ **本机最干净的推送路径是 SSH（端口 22），不需要任何代理**。已生成密钥对
`C:\Users\s1706\.ssh\id_ed25519`（ed25519，无口令），公钥：
`ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIOV6JOvj+tpPGNjQu6NufJB9qyGjTYra1UpZ7hCs9O2d WRw5w-jinyinsai1-20260922`
—— **待用户把它注册到 GitHub 后，`git push ssh-origin main` 即可完成推送。**

**已完成的本地准备（与凭据无关，全部就绪）**：
- 提交 `4e35b95`「复赛交付：解析定形求解器、三层校验与三件交付物」：36 文件、+3904/−77。
  含 `round_shaper.py`、`build_semi_plan.py`、`data/semi/cap00{2,5,0}.config.json`、
  `deliverables_semi/`（7 件）、`diagnostics/`（新增 8 个）与记忆文件。
  **故意不含** `submission_semi_test/`（一次打包试跑的遗留副本，无源码引用它）。
- 相对远端 **领先 1、落后 0** → 干净快进，无需 merge。
- 新增 remote `ssh-origin` = `git@github.com:WRw5w/jinyinsai1.git`（**保留原 `origin` 不动**，
  以便将来在有代理的网络里仍能走 HTTPS）。
- `tmp/push_via_api.py`（新）：走 `api.github.com` 的 Git Data REST API 推送器，作为
  "不想配 SSH key、愿意给 PAT" 的等价路径。**已 `--dry-run` 全链路验证通过**（只读、
  免 token）：读到远端 ref → base tree `a1405f6d5ee5` → 36 个变更（中文路径正确解码，
  用 `git diff --name-status -z`）→ 1,714,430 B 原始 / 2,285,940 B base64。
  正式跑：`GH_TOKEN=<fine-grained PAT, Contents: Read and write> python -X utf8 tmp/push_via_api.py`。

**踩坑**：
1. HTTPS 那条 `git push origin main` 会**挂死**并把整条 Bash 命令拖到被沙箱 SIGTERM；
   要单独跑并加 `GIT_HTTP_LOW_SPEED_LIMIT=1 GIT_HTTP_LOW_SPEED_TIME=8`，或直接别试。
   SSH 那条必须 `-o BatchMode=yes` + `GIT_TERMINAL_PROMPT=0`，否则挂在交互提示上。
2. **`git ls-remote` 报 502 ≠ 网络全断**。它走进了沙箱代理；裸 socket 到 22 是通的。
   诊断要**按端口**测（Python `socket.create_connection`），不要用 git 的报错反推全局。
3. 本仓库的提交作者是 `Zhang Chengxi <zhangchengxi@example.com>`（**占位邮箱**），
   推上去不会归属到任何 GitHub 账号。若要归属正确，push 前应先改
   `git config user.email` 为该账号已验证的邮箱并 `--amend --reset-author`。
4. **用户手上另有一把已有公钥**（comment `19811-github`，指纹
   `SHA256:dMhxCjFUVbyXIBDZh0luBcTuEQlYYPd97C91FcaA6O8`），**不是本机这把**
   （本机 `id_ed25519` 指纹 `SHA256:CM5mRX7Or6u8eP7mtk5aqzOxc1B5db88Csiu/1Tsi4E`，
   私钥在 `C:\Users\s1706\.ssh\`，已验证配对）。**公钥必须与要推送的那台机器上的私钥配对**，
   所以往 GitHub 贴的必须是本机这把；那把 `19811-github` 是别的机器的，贴上去本机也推不动。
   两把可并存，互不影响。核对工具：`ssh-keygen -l -f <pubfile>`（GitHub 设置页也显示同样的
   `SHA256:` 指纹）。

### 10. 本机清点：确认这是"两台机器并行"里的第二台（09-22 11:1x）

用户说"我有两个机器并行推进，你这边是第二个"，要求查本机。**查清了：**

| | 机器 1（原始开发机） | 机器 2 = 本机 |
|---|---|---|
| 工作目录 | `D:\02_Projects\ML\jinyinsai1` | `C:\Users\s1706\WorkBuddy\2026-09-21-20-20-26\jinyinsai1` |
| 轮次 | **初赛**（已达官方 100.0000） | **复赛** |
| SSH 公钥注释 | `19811-github` | 2026-09-22 新生成 `id_ed25519` |
| 编译器 | `D:/tmp/msys64/ucrt64/bin/g++.exe` | `C:\mingw64` |

**证据来源**：`HANDOFF.md` 第 292 行写明机器 1 的工作目录（该文件由机器 1 写给接手者，
1000+ 行、全是初赛内容）；本机 `WorkBuddy\2026-09-21-21-20-21\复赛迁移核对表.md` 是机器 2 自己在
09-21 21:40 写的初赛/复赛差异核对表。**两台机器唯一的同步点就是 GitHub 仓库。**

**本机清点结果（事实）**：
- C: 只有一个用户配置 `s1706`；**只有一个 git 仓库**（`D:` 盘浅扫 depth≤3 无第二份副本，
  `D:\Users\s1706` 下只有 AppData）。
- `~/.ssh` 里**只有我刚生成的那一把**（`id_ed25519` + `.pub` + `known_hosts`）→ 证实
  `19811-github` 的私钥不在本机。无 `~/.gitconfig`、无 `.git-credentials`。
- WorkBuddy 工作区 7 个：`-13-14-02`（VS Code/ACM 环境搭建，1.36 GB）、`-19-32-20`（激活脚本）、
  `-19-52-07`/`-19-54-40`（空）、**`-20-20-26`（仓库所在，613 文件）**、`-21-20-21`（机器 1 会话恢复 +
  核对表）、`-09-24-53`（今日）。

**远端此刻**（api.github.com）：只有 `main` 一个分支，3 个提交（09-21 12:11–12:20 UTC），
HEAD 仍 `4e14c6c` → **机器 1 近 23 小时没有推新东西**，我方 `4e35b95` 仍是干净快进。

**对照 `复赛迁移核对表.md` 的 5 项待办，现状**：
1. ✅ 已补 —— `platform_score.py` 现含 `numerator_capped_by_demand` / `penalise_short_delivery` /
   `credited_finished` / `under_delivery` / `missing_order` / `demand_kg`（核对表说的注释与代码
   不一致已修掉）；
2. ✅ 已做 —— 时间子分三读法已在报告 §6.3 priced；
3. ✅ 已完成 —— `deliverables_semi/` 三件套齐；
4. ❌ **仍未堵** —— `build_submission.py:229` 的 `--data` 默认还是 `data`（初赛目录），
   只靠报告 §8 的文字警告，**代码层没有断言**。这是随代码包一起交出去的真实绊脚石；
5. ✅ 已改路线 —— 放弃 `solve_semi.py` 分块搜索，改用解析定形（§4）。

**建议（待用户决定，推之前做最好）**：① 给 `--round semi` 加 `--data` 默认值/断言；
② 给 `HANDOFF.md` 追加一节复赛交接（现在 0 次提到复赛，机器 1 拉下来看不到进展）。
⚠️ **两台机器都在改同一批文件，git 只能解文本冲突、解不了语义冲突** —— 这两处改动要先确认
机器 1 没有在改同文件，否则会制造冲突。

### 11. ✅ 推送成功（09-22 11:2x）

用户把本机公钥加到 GitHub 后，**认证一次通过**（`Hi WRw5w! You've successfully authenticated`），
随即推送成功：

```
4e14c6c..4e35b95  main -> main          # git push ssh-origin main, exit=0
```

**三重独立验证**（不只信 `git push` 的退出码）：
1. `api.github.com/repos/WRw5w/jinyinsai1/commits/main` → sha `4e35b9551a3730fe7418443440a19ff781eb22af`，
   提交信息与时间（`2026-09-22T02:45:28Z`）与本地一致；
2. `git fetch ssh-origin` 拿到真实对象；
3. 递归 tree API 核了 **315 个文件**，9 个关键交付物字节数**逐个吻合**：
   `复赛结果_棒材优化.zip` 122,000 / `.json` 803,524 / `复赛代码与模型.zip` 426,986 /
   `复赛技术报告.md` 38,757 / `环境与复现说明.md` 7,782 / `diagnostics/semi_headroom.py` 9,408 /
   `diagnostics/semi_lam_sweep_r010.txt` 3,756 / `round_shaper.py` 15,109 / `build_semi_plan.py` 9,011。

**踩到的坑（重要，已写进用户级备忘）**：`git update-ref` 与 `git fetch` 对
`.git/refs/remotes/**` 的写入**返回 rc=0 但静默不落盘**（该目录始终是空的），
于是 `origin/main` 一直读到 `packed-refs` 里的陈旧值 `ae8b675`，`git status` 误报 `ahead 2`。
`.git/refs/heads/**` 可写（commit 正常）。**绕过**：Python 建目录 + 直接写松散 ref 文件；
写完 `rev-parse` / `status` 全部归位（`## main...origin/main`，无 ahead/behind）。
→ **本地 git 报成功 ≠ 状态变了，ref 操作改完必须回读校验。**

**留下的两个可选后续**（已向用户提出，未执行）：
① `build_submission.py` 的 `--data` 默认值仍是 `data`（初赛目录），`--round semi` 不自动切换，
   代码层无断言 —— 随代码包一起交出去的真实绊脚石；
② `HANDOFF.md` 被跟踪但 0 次提到复赛 —— 机器 1 拉代码看不到复赛进展。
两处都动两台机器共用的文件，动手前需确认机器 1 没在改同文件。

### 12. ✅ 复核更正：λ 的选点依赖基准刀数（09-22 12:0x，提交 `c3332bb`）

**起因**：用户先要了一份"精确值核对"（无约数），随后转来机器 1 的复核意见
（机器 1 声称"已无优化空间"不成立，并做了一个受限换配对实验），最后说"开干"，
让我把复核暴露的问题真正改掉。

**本轮最重要的发现（推翻了我自己前稿的结论）**：
**λ 的选点依赖基准刀数 B，不是一个不变量。** 刀分子分是 `40·min(1, B/K)`，
"每把刀值多少分"随 B 线性放大；一旦 B≥K 封顶，目标就退化成纯成材率。
所以"λ=2400 在四种口径下都不劣"这句话是**错的** —— 它只在 B=90k 附近成立。
新增 `diagnostics/semi_lam_baseline_sensitivity.py`（11 点 λ × 3 基准 × 2 口径）实测：

| 家族 | 最优 λ | 与交付点(λ=2400)的差 |
|---|---:|---:|
| floor @ 90k | 2400 | 0（就是交付点） |
| floor @ 160k | 4800 | 0.055195 |
| floor @ 180k | **1200** | **0.213400** |
| nearest @ 90k | 1600 | 0.041734 |
| nearest @ 160k / 180k | 2400 | 0 |

**更正的三处（都是我前稿的真错，不是措辞问题）**：
1. **§0 摘要 + §9**：删掉"λ 选点在全部读法下不变 / 已达全局最优"，改为"选点依赖 B，
   在 B=90k 附近不变"，全局结论降级为**稳定局部最优**。
2. **§6.5 证据(2)**：把"配对窗口 4→8 是**不动点**"降级为"**贪心策略饱和**"。理由：窗口加倍后
   12,559 个回合逐条一致，只能说明**当前贪心策略在自身参数下饱和**，不能说明配对空间里没有
   更好的解 —— 机器 1 的换配对实验就是反例。同时补了已知的收窄点表
   （`build_semi_plan.py:59` 遇到首个可行配对即停、`round_shaper.py:137` 每轮同一并排支数），
   并指出"相邻方案两两共享率 100%"说明瓶颈在**整根取整**而非可配性。
3. **§7-5 未进方案的订单**：前稿描述错了。真身是 **B20275738**（C60 / φ26.5 / 4.9 m，
   方案 #207，同规格 3 单），它**只参与自己方案的第 5 轮**，不是一个"未配对的单订单方案"。

**新增/降级两节**：
- **§6.7（新）**：λ 选点对基准刀数的敏感性，含 11 点曲线表 + 6 行选点差表。
- **§7-6**：把机器 1 的换配对见证降为**量级参考**（+0.0002~0.0003 分，**低于 0.01 的显示量化**，
  即评审看不出差别），并标注**风险**：其 `scoped_data` 可能把 B20270281 藏起来了，
  和我们的"异常单必须显式排除"口径不一致。
- **§7-8（新）**：优先级路线图 —— **口径 → λ/r 重选 → 配对/异构轮次 → 全局重排**。
  把"先做换配对搜索"往后放：口径（floor vs nearest，值 1.55~2.75 分）比换配对的
  0.0003 分大四个数量级，**先把口径钉死再谈优化**。

**数字一致性**：方案数 5,000 → **2,196** 全文统一（`merge_compatible` 装箱合并后的结果）。

**交付物状态**（本轮**只动文字 + 新增一个诊断脚本，未动交付方案本身**）：
| 文件 | 字节 | 变化 |
|---|---:|---|
| `复赛代码与模型.zip` | 430,921 | 重打（新增 `diagnostics/semi_lam_baseline_sensitivity.py/.json`），sha `6fc31f03…` |
| `复赛技术报告.md` | 49,607 | 9,412 汉字 / 59 表 |
| `环境与复现说明.md` | 8,839 | 目录树 + 复现步骤同步 |
| `提交说明.md` | 3,266 | 补 λ-依赖-B 说明 |
| `复赛结果_棒材优化.zip` / `.json` | 122,000 / 803,524 | **哈希未变**（`52a1b004…` / `87b9812e…`）→ `validation_report.json` 仍有效 |

`tmp/verify_deliverables.py` 19 项断言**跑两次均 PASS**。

**提交与推送**：提交 `c3332bb`（6 文件，+490/−40），SSH 推送成功
`4e35b95..c3332bb  main -> main`。**三重核验**：① API `commits/main` → `c3332bb…`；
② 递归 tree 317 blob；③ 6 个变更文件 `git rev-parse HEAD:<path>` 与远端 blob sha **逐一相符**
（`87ea80602164` / `d220e6f82f4a` / `02506bafef53` / `dcd09120d114` / `36a248fa88ea` / `23aa6a858344`）。
远端 `origin/main`、`ssh-origin/main` 松散 ref 已同步到 `c3332bb`。

**本机踩坑（新增，写进用户级备忘）**：
1. **`Add-Type` 被沙箱禁**（"Add-Type compiles and loads .NET code at runtime"）→
   想在 PowerShell 里读 zip 目录行不通，改用 **Python `zipfile`**。
2. **Python `urllib` 访问 api.github.com 报 `SSLEOFError`**，但同一环境 PowerShell
   `Invoke-RestMethod` 正常 → **本机查 GitHub API 要用 `Invoke-RestMethod`**。
3. Bash 工具缺 coreutils（`dirname`/`head`/`cd` 全无）→ 一律 PowerShell + 重定向到文件再 Read。

**仍未核实（如实标注，不要当成已确认）**：
机器 1 引用的四份证据 —— `optimization_review_probe.py`、`probe_report.json`、
`witness_result.json`、`RULES_SOURCES.md` —— 在本机（`D:\02_Projects\ML\jinyinsai1` 不存在）
和 GitHub（`main@c3332bb` 全树 362 条，正则 `optimization_review|probe_report|witness_result|RULES_SOURCES`
**零命中**）都找不到。所以那个换配对见证目前**只是量级参考**，没有独立复算过。
**160,000 这个基准也没有档案来源**（`RULES.md` §59 只记初赛 90,000；报告 §6.3 写"复赛未公布"）
→ 报告里只当**敏感性端点**用，不当结论。若机器 1 能给出出处（日期 + 原文），
则应改按 **floor@160k → λ=4800** 重选，届时 `json_sha256` 会变、`validation_report.json` 需重出。

### 13. ✅ 结论：160,000 的出处已入仓库（09-22 20:1x）

用户告知"160000 的证据已经上传上去了"。实际是**机器 1 的提交 `40ffc33`**
「Add evidence for semi-final knife baseline」，在 main 上加了两份文件：

| 文件 | 字节 | 内容 |
|---|---:|---|
| `evidence/rules/BASELINE_160000_SOURCE.md` | 863 | 160000 的出处说明 |
| `evidence/rules/RULES_SOURCES.md` | 13,527 | 三类规则源（PDF / constraints.txt / 群答疑）溯源档案 |

**关键事实**：复赛基准刀数 **160000** 出自**群答疑 09-21 15:46「★ 复赛基准刀数 = 160000」**，
由 WorkBuddy 会话 `21624865-44ed-450b-a9b2-c650dec10933`（35.8 MB / 8128 行）导出摘录。
⚠️ **不是 PDF、也不是 `constraints.txt` 明文**——基准值**只有群答疑给**（PDF 只给了初赛口径的
公式且已确认有误，constraints.txt 只写约束不写计分）。

`RULES_SOURCES.md` 的效力排序（可复用）：**约束「是什么」→ constraints.txt 权威；评分「怎么算」
与基准值 → 只有群答疑**；PDF 在复赛阶段**已被群答疑部分废止**（09-21 公告「赛题文件中与求解
时间相关的表述作废，复赛和半决赛无须考虑求解时间」）→ 求解时间不再是约束，10 秒预算失去依据。

同档案的其它有用事实：复赛交付物清单含 **README**；最终代码需 **Linux 下测试通过**；允许
NumPy/Pandas/Geatpy，商业求解器**仅限 COPT**，禁止封装式 API 直接输出结果；半决赛数据量
**2 万单**（属**另一赛段**，非本轮）；`blank_used_finals.csv` 实为 **5 种坯型**（机器 1 更正了
此前"2 种"的误判，源于把它和初赛的 `blank_used.csv` 弄混）。

**处置（已完成）**：报告 §2.4 / §6.3 / §6.7 / §7-2 / §9 与 `提交说明.md` 全部删掉"160,000
无规则来源、只当敏感性端点"的旧说法，改为「群答疑口径、非书面、但为**当前最可信取值**，
90,000 退为历史校准锚点」，并写明采纳 160k 的三重后果（总分 78.0006→93.91399713057804
（floor）/ 76.4539→91.16430524395439（nearest）；刀口径之差 1.5467→2.74969188662367 分；
λ 最优点 2400→4800）。
**交付仍保留 λ=2400**：刀口径（值 1.55~2.75 分）比 floor@160k 的 0.055195 分**大两个数量级**，
口径未定前不值得为 0.055 分换包；2400 是 (nearest,160k) 与 (nearest,180k) 的族内最优。

尺寸回填：报告 **51,250 B / 9,677 汉字**，提交说明 **3,697 B**；结果包 / JSON / 代码包哈希**均未变**
（`52a1b004…` / `87b9812e…` / `6fc31f03…`）；`tmp/verify_deliverables.py` **19/19 PASS**。

### 13.1 事故：`git rebase` 把本地 `.git` 打坏（已修复，有备份）

想把日志提交 rebase 到 `40ffc33` 之上，`git rebase 40ffc33b` 报
`could not mark as interactive: No such file or directory`，随后状态崩坏：
`refs/heads/main` **文件消失**（`git log` → "No commits yet"）、`cat-file` 读不到**任何**提交对象。
`.git` 只剩 13 个松散目录 + 一个 5.3 MB pack，`packed-refs` 里 `origin/main` 还是陈旧的 `ae8b675`。

诊断过程：`git fetch --refetch` 补回大部分对象（`f800829` 恢复、历史能走到 `f800829`），但
`c4bf187c` 仍缺、`git reset --mixed` 报 `unable to read e19c458`。**但干净克隆同样不含
`e19c458`/`c4bf187c` 且 `fsck --connectivity-only` 通过** → 说明这两个对象本就不在任何分支
可达范围内，是旧索引里的残留引用，不是远端缺件。

修复：`git clone --no-checkout git@github.com:WRw5w/jinyinsai1.git tmp/reclone` → fsck 通过 →
旧 `.git` 移到仓库外 `…\2026-09-21-20-20-26\jinyinsai1_git_broken_20260922` → 克隆的 `.git` 移入 →
重写 `.git/config`（`origin`=HTTPS、`ssh-origin`=SSH、`user`=Zhang Chengxi、`quotepath=false`）→
`git reset --mixed HEAD` 重建索引 → `git checkout -- evidence/rules` 补出机器 1 的两份文件。

**结论：本机沙箱下 `git rebase` 不可用**（它改引用会静默失败，且这次直接删掉了
`refs/heads/main`）。要线性历史就手动做：`reset` 到目标提交、重新提交工作区文件，
**不要再试 rebase**。恢复后复核：6 个交付物 sha256 **全部未变**。

### 13.2 ⚠️ 发现机器 1 的竞争分支 `semi-final`（未决，需用户拍板）

远端除 `main` 外还有 `semi-final` 分支（`35cf8a1`），与 main 在 `4e14c6c` **分叉**，
main 领先 4 / 落后 7。它是**另一条独立工作线**（输入是
`D:\02_Projects\ML\jinyinsai1_nolimit\runs\semi_nolimit_v1\result.json`），带**一整套竞争性交付包**：

| | `main`（本机线） | `semi-final`（nolimit 线） |
|---|---|---|
| 结果包 | `deliverables_semi/` zip 122,000 B | `submission_semi_nolimit/` zip 145,537 B |
| 计刀口径下刀数 | **175,952** | 185,648 |
| 轮数 | 12,559 | 11,089 |
| 方案数 | 2,196 | 1,941 |
| 成材率 | **91.80813635372822%** | 91.07929585% |
| 申报坯重 kg | **545,633,119.756** | 549,999,419.612 |
| 覆盖率（对源） | 99.99% | 99.99% |
| 违规 | 0 | 0 |
| 总分 @160k | **≈93.913997**（floor） | 91.797632 |

两包 `finished_weight` 几乎相同（均按需求封顶，500,935,598.58 kg），差异全部来自**申报坯重**。
**在同一个 `platform_score`（floor 口径）下 main 领先约 2.12 分**；但若平台真实口径是
`nearest`，main 的刀数会变成 190,341（差于对方的 185,648）→ **两包互有胜负，取决于未决的
计刀口径**。这是**交付物二选一**的风险点，需用户与机器 1 明确以哪个分支为准。

