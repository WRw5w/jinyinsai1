# `jinyinsai1` 包内两条交付线对比（对方 AI vs 我们）

> 生成时间：2026-09-23　　范围：`D:\02_Projects\ML\jinyinsai1`（远端 `github.com/WRw5w/jinyinsai1.git`）
> 对比对象：该包里的**另一条 AI 交付线**（`main`）与本机这条线（`semi-final` + 工作树）
> 约束：本报告只做**方法与证据**的对照，不搬用对方任何方案数据（用户长期约束：「不许抄对面的答案，保持多样性」）

---

## 0. 一句话结论

对方在 `jinyinsai1` 包里交出的最好方案是 **93.914**（floor 口径 / B=160,000）；
我们本机线上的最好方案 **94.712**，**净领先 0.798 分**——而且我们是在**成材率低 1.71 个百分点**的情况下，
靠**少 6,167 刀（−3.5%）**赢回来的。

但同一份对比暴露出一个**比 0.8 分更大的风险**：刀的取整口径两方结论相反，
这一项值 **2.75 分**，而且它**会翻转我们自己的包排序**（详见 §4）。

---

## 1. 为什么这个包里会有"两个 AI"

两条线的 **git identity 完全相同**（都是 `Zhang Chengxi <zhangchenxi@example.com>`），
提交时间也都在 09-22。**只能靠输入路径和机器名分辨**：

| 判据 | `main`（对方） | `semi-final`（我们） |
|---|---|---|
| 分叉点 | `4e14c6c`（09-21，"Add semi-final dataset archive"） | 同左 |
| 分叉后提交数 | **2 笔**（09-22） | **8 笔**（09-22） |
| HEAD | `40ffc33` | `35cf8a1` |
| `提交说明.md` 里的输入路径 | `C:\Users\**s1706**\WorkBuddy\2026-09-21-20-20-26\jinyinsai1\runs\capx_0.10\result.json` | `D:\02_Projects\ML\jinyinsai1_nolimit\runs\semi_nolimit_v1\result.json` |
| 交付目录 | `deliverables_semi/` | `submission_semi_nolimit/` |
| 日志里的自述 | `.workbuddy/memory/`（`会话恢复摘要` 明写「原始工作区…**另一台机器**」） | `D:\…\jinyinsai1_nolimit\.workbuddy\memory\` |

即：对方是**另一台机器**（WorkBuddy 沙箱会话 `2026-09-21-20-20-26`、用户 `s1706`）上跑出来的完整交付，
被推到了同一个 GitHub 远端；本机这条线是**我们的 fork 路线**，也推到了同一远端的另一条分支。

> 注：`jinyinsai1` 工作树里还有一批**未跟踪**文件（`repair_semi.py`、`watch_semi.py`、`tools/`、
> `diagnostics/optimization_review_20260922/`、根目录 `复赛结果_棒材优化_待提交.zip` 122,000 B），
> mtime 全部是 09-22 20:10 —— 与一次工作区搬运/检出同一时刻，其中 `tools/` 是我们打榜链路那套。
> 它们**不属于对方的 git 提交**，是散落文件。

---

## 2. 对方产物清单（`main`，`deliverables_semi/`，7 件）

| 文件 | 字节 | 说明 |
|---|---:|---|
| `复赛结果_棒材优化.zip` | 122,000 | 根目录只含同名 JSON |
| `复赛结果_棒材优化.json` | 803,524 | 明文 |
| `复赛代码与模型.zip` | 430,921 | **字节可复现**（固定时间戳 + 按名排序） |
| `复赛技术报告.md` | 49,607 | **43 节 / 45 表**，每个数字都有落盘证据 |
| `提交说明.md` | 3,245 | 自动生成 |
| `validation_report.json` | 7,345 | 三层复验 + SHA |
| `环境与复现说明.md` | 8,839 | 手写 |

**对方的方法（核心）**：`build_semi_plan.py` 的四步分析法
「**按钢种/规格分组 → 组内配对 → 逐方案重定形（`round_shaper.py`） → 双目标选点**」，
外加 **9 点 λ 扫描** + **6 点超产允量 r 扫描**；**纯 Python 标准库**；
**全卷构建实测 ≈ 76 s**（`prepare 0.44 s + 求解 73 s + 打包 2.1 s`），且**重跑逐字节可复现**。

**对方自报数字**（B=160,000）：

| 指标 | floor 口径 | nearest 口径 |
|---|---:|---:|
| 刀数 | 175,952 | 190,341 |
| 成材率 | 91.8081% | 91.8081% |
| 覆盖率 | 99.9900% | 99.9900% |
| 轮数 / 打包后方案数 | 12,559 / 2,196 | 同左 |
| **总分** | **93.9140** | **91.1643** |

---

## 3. 我们这条线的最好成绩（同一评分器、同一基准）

全部用官方口径 `40×min(1, 160000/K) + 30×Y + 20×C + 10` 重算（子分先四舍五入两位）：

| 包（`jinyinsai1_nolimit/`） | 轮数 | 刀数 floor | 刀数 nearest | 成材率 | 覆盖率 | **floor 分** | **nearest 分** |
|---|---:|---:|---:|---:|---:|---:|---:|
| `submission_semi_nolimit` | 11,089 | 185,648 | 197,569 | 0.910793 | 1.0000 | 91.797 | 89.717 |
| `submission_semi_safety`（**就是对方那版**，我们留作保底） | 12,559 | 175,952 | 190,341 | 0.918081 | 0.9999 | 93.914 | 91.164 |
| `submission_ours_seed` | 11,360 | 175,731 | 186,376 | 0.917885 | 0.9999 | 93.953 | **91.874** |
| `submission_ours_94` | 11,132 | 173,446 | 186,260 | 0.904656 | 0.9997 | 94.032 | 91.494 |
| **`submission_semi_merged_v2`** | **10,742** | **169,785** | 185,495 | 0.900961 | 0.9994 | **94.712** | 91.517 |

**归因拆解**（`merged_v2` vs 对方包，floor 口径）：

| 分项 | 对方 | 我们 | 差 |
|---|---:|---:|---:|
| 刀数子分 | 36.374 | 37.695 | **+1.321** |
| 成材率子分 | 27.542 | 27.029 | **−0.513** |
| 覆盖率子分 | 19.998 | 19.988 | −0.010 |
| 时间子分 | 10 | 10 | 0 |
| **总分** | **93.914** | **94.712** | **+0.798** |

**读法**：我们走的是「**用成材率换刀数**」的路线，对方走的是「**保成材率**」的路线；
在 B=160,000（刀项**未封顶**，K≫160k）下**刀数更值钱**，所以我们的方向赢。
另外我们少了 **1,817 轮**（10,742 vs 12,559），这本身就是刀数的直接来源。

**我方的岛（今天 10:44 实测）**：

| 岛 | 块完成 | 状态 |
|---|---|---|
| `semi_anchor_v1` | 183/183 | ✅ |
| `semi_anchor_s2` / `s3` / `s4` | 184/184 | ✅ |
| `semi_nolimit_v1` | 183/183 | ✅ |
| `semi_anchor_60` | **124/184** | ⏳ 运行中（elapsed 2,576 s） |
| `semi_refine_s1` | **118/183** | ⏳ 运行中 |
| `semi_refine_s2` | **118/183** | ⏳ 运行中 |

`submission_semi_merged_v2` 是今天 10:32 用**已完成的 5 个岛**合的。
剩下 3 个岛跑满后再合一次，按「逐块择优对候选集单调」的既有结论，**只会更好**。

---

## 4. ⚠️ 最大发现：取整口径两方结论相反，值 2.75 分，且会翻转我们的包排序

### 4.1 两方的立场

**对方（报告 §6.6，标题「主导一切的一项」）**：采用 **`nearest`**。
理由是**一致性论证**——`platform_check` 若对交付用 `int(L//size)`，则 39.8% 的 (k, 定尺) 组合都会被判「少交 1 件」，
几乎所有方案都会爆违规；既然平台对**交付**必须宽容，它对**计刀**大概率也宽容。
对方同时**明确声明刻意不去利用** floor 折扣（把长度写到整数倍下方 1e-15）。

**我们（`solver.py:194–215` + `platform_score.py:160–177`）**：采用 **`int(L//size)`（floor）**。
依据是**实测复现**——2026-09-16 官方 **92.02** 那次反馈，
平台回报 `108671 = Σk(k≥2) + 2·#{k==1}`（35718 段），
正好等于 `max(2, int(L//size)+1)` 的口径；这条observation是**有判别力的**：
去掉 `max(2,…)` 下限后模型预测 86751，其余四份反馈仍然全过，说明它不是拟合出来的常数。

### 4.2 我实测的事实（今天）

我方各包的 floor / nearest 刀数差：

| 包 | floor | nearest | 差 | 相对 |
|---|---:|---:|---:|---:|
| `merged_v2` | 169,785 | 185,495 | **+15,710** | **+9.25%** |
| `ours_94` | 173,446 | 186,260 | +12,814 | +7.39% |
| `ours_seed` | 175,731 | 186,376 | +10,645 | +6.06% |
| `semi_nolimit` | 185,648 | 197,569 | +11,921 | +6.42% |

**机制已查清，不是刻意构造**：折扣来自**十进制字面量的双精度除法**。
例：`24.5 // 4.9 → 4`（因为 `24.5 / 4.9 = 4.999999999999999`），
`14.445 // 4.815 → 2`。JSON 里写的是 `24.5`、`4.9` 这种「看起来是整数倍」的值，
平台只要用**双精度**做 `int(L//size)`，就会和我们算出同样的折扣。

**据此判断**：对方的顾虑（平台会不会对交付也 floor）其实与计刀口径**不冲突**——
平台完全可以「**交付判定宽容 + 计刀用 floor**」，而我们的 92.02 证据恰好就是这个组合。
所以 **floor 仍是我们更愿意下注的一侧**，但这已是**下注**，不是定论。

### 4.3 真正的后果：**包排序会翻转**

同一批包，换口径重排：

| 包 | floor 分 | nearest 分 | **minimax（取两者最差）** |
|---|---:|---:|---:|
| `submission_semi_merged_v2` | **94.712** | 91.517 | 91.517 |
| `submission_ours_94` | 94.032 | 91.494 | 91.494 |
| `submission_ours_seed` | 93.953 | **91.874** | **91.874** ← 最稳 |
| `submission_semi_safety`（对方） | 93.914 | 91.164 | 91.164 |
| `submission_semi_nolimit` | 91.797 | 89.717 | 89.717 |

- 按 floor：`merged_v2` 第一，比 `ours_seed` 高 **0.759**。
- 按 nearest：**`ours_seed` 反超 `merged_v2` 0.357**。
- **最小后悔（minimax）最优是 `ours_seed`，不是 `merged_v2`。**

### 4.4 建议的判定实验（成本可控，且规则允许）

赛程是「**每天 5 次提交、取复赛阶段最高**」（`RULES.md` §11.1）。
所以**同一天把 `merged_v2` 与 `ours_seed` 各提一发**，榜面回分就能直接判出口径：

- 若 `merged_v2` ≈ 94.7、`ours_seed` ≈ 93.95 → **floor 成立**，之后全押 floor 路线；
- 若 `merged_v2` ≈ 91.5、`ours_seed` ≈ 91.87 → **nearest 成立**，必须立刻改口径重跑（见 §5.1）。

这一发实验的信息价值（2.75 分的口径折算 + 决定后续所有选点）远高于它占用的 2/5 额度。

---

## 5. 值得吸收 vs 明确不采纳

### 5.1 ✅ 值得吸收（方法层，零抄答案成分）

1. **「口径 × 基准」二维敏感性要显式入档。**
   对方 §6.7 用 11 点 λ × 2 刀口径 × 3 基准（≈70 s）证明了
   **λ 的权衡最优点随基准 B 移动**（B 变大 → 每刀更值钱 → 最优点右移；刀项封顶后左移）。
   → 我们对应的问题是：`diagnostics/merge_chunks.py:44` **硬编码 `knife_convention='platform_floor'`**，
   逐块择优用的 `_key`（`solver.py:651`）也是 floor 口径的分数。
   既然候选排序会随口径翻转（§4.3），就应当**再用 `'intended'` 合一次**，
   得到「nearest 口径下的最优合并包」，然后按 minimax（或按 §4.4 的实验结论）二选一。
   `diagnostics/chunk_global_ab.py:124` 已经有 `knife_convention='intended'` 的用法，**这是一处配置改动，不是重写**。

2. **交付包的工程规范两条**：
   - 代码包**字节可复现**（固定 `ZipInfo.date_time` + 按名排序 + 固定 `external_attr`），
     重打两次哈希不变 —— 这是评审能核对「拿到的是同一份代码」的前提；
   - **报告里每个数字都必须有一条能重跑的落盘证据**，只存在于临时 stdout 的数字等于不可复核。
   （对方的 `diagnostics/semi_*` 一整套脚本就是这条规范的执行；我们自己也有 `diagnostics/`，建议对齐。）

3. **对手方案已被独立审计证明非局部最优 —— 说明「确定性分析法」在配对邻域上没走到不动点。**
   `diagnostics/optimization_review_20260922/probe_report.json`
   （本机 09-22 20:10，`source_commit=4e35b95`、`source_sha256=87b9812e…`）对
   `deliverables_semi/复赛结果_棒材优化.json` 做了 **1 个邻域**的 pair-exchange，就找到改进 witness：

   | 量 | 改进前 | 改进后 |
   |---|---:|---:|
   | 配对（`B20276141/42` × `B20275965/66` 交叉换伴） | — | — |
   | pair metrics | `(101, 394,153.5)` | `(100, 391,935.0)` |
   | 原始投坯 kg | 545,633,119.756 | **545,630,901.256**（省 2,218.5 kg） |
   | 刀数 floor / nearest | 175,952 / 190,341 | **175,951 / 190,340** |
   | floor 分 | 93.913997 | **93.914316** |
   | nearest 分 | 91.164305 | **91.164594** |
   | `platform_check` | — | **通过（errors = {}）** |

   `neighborhoods_tried = 1` —— 只试了**一个**邻域就赢，说明继续搜还有空间。
   这条佐证了「**岛屿/邻域搜索**」这条路线本身是对的（也正是我们的路线），
   只不过我们的规模（逐块 60 单 × 183 块 × 7 岛）已远超它。

4. **对方的 43 节技术报告骨架**（尤其 §6.5 剩余空间分解、§6.6 口径、§6.7 选点敏感性）
   是一份结构上很干净的可交代模板；我们的交付说明还是轻量的，**最终交付前值得对齐到同一档次**
   （报告本身要重写，用我们自己的数字）。

### 5.2 ⛔ 明确不采纳

| 项 | 理由 |
|---|---|
| 抄对方的方案/参数（λ=2400、r=0.10） | 用户长期约束；且我们的搜索路线与它不同源，套参数无意义 |
| 转向对方的「1 分钟分析法」 | 它自己的包已被单邻域审计打破（§5.1-3），且其最好成绩低于我们 0.798 |
| 利用 floor 折扣「写到整数倍下方 1e-15」 | 双方都拒绝这条路；那是攻击评分器的浮点表示，不是解题 |
| 对方的交付文件名 `复赛结果_棒材优化.zip` | 平台按**队名**校验，我们的队名是 **鱼不吃猫**（`AIC-2026-93096493`），应保持 `复赛结果_鱼不吃猫.zip` |

---

## 6. 对方报告里的一处自相矛盾（值得知道，但不是我们能改的）

对方报告 **§6.7 的注意事项**写着：

> ⚠️ 复赛**未公布**基准刀数……本仓库内**检索不到 160,000 的任何规则来源**……
> 因此本报告只把它当作**敏感性端点**列出，不作为结论依据。

但 `main` 分支的**最后一笔提交** `40ffc33`（"Add evidence for semi-final knife baseline"）
恰恰就是补上 `evidence/rules/BASELINE_160000_SOURCE.md`，
里面摘录了 09-21 15:46 的群答疑原文 **「★ 复赛基准刀数 = 160000」**（发言人：中文名字）。

即：**报告与自己的证据提交错开了** —— 报告把 160k 当不确定端点，而同一分支末尾已把它入档。
这对我们的意义：**B=160,000 这条我们一直在用的口径是对的**，不必因为对方报告的措辞而动摇。
（我们自己的 ROBUST 来源在 `evidence/rules/RULES_SOURCES.md`：官方 PDF → 复赛 `constraints.txt` → 群内答疑。）

---

## 7. 证据位置

| 内容 | 路径 |
|---|---|
| 对方交付物（git） | `jinyinsai1` 分支 `main`（`40ffc33`）→ `deliverables_semi/` |
| 对方技术报告 | `git show main:deliverables_semi/复赛技术报告.md`（§0 / §3 / §6.5 / §6.6 / §6.7 最值钱） |
| 对方 160k 证据 | `git show main:evidence/rules/BASELINE_160000_SOURCE.md` |
| 对方工作日志 | `jinyinsai1/.workbuddy/memory/2026-09-2{0,1,2}.md`、`会话恢复摘要_2026-09-16至09-21.md` |
| 对方包的独立审计 | `jinyinsai1/diagnostics/optimization_review_probe.py` + `…_20260922/probe_report.json` |
| 我方包 | `jinyinsai1_nolimit/submission_semi_{merged_v2,ours_94,ours_seed,nolimit,safety}/` |
| 我方口径证据 | `jinyinsai1_nolimit/solver.py:194–215`、`platform_score.py:160–177` |
| 我方逐块择优 | `jinyinsai1_nolimit/diagnostics/merge_chunks.py`（第 44 行口径、`_key` 判据） |
| 我方岛状态 | `jinyinsai1_nolimit/runs/semi_*/progress.json` + `chunks/**` |

---

## 8. 下一步（按优先级）

1. **等 3 个岛跑满**（`semi_anchor_60` 124/184、`semi_refine_s1` 118/183、`semi_refine_s2` 118/183），
   跨 7 岛重跑 `diagnostics/merge_chunks.py` → 新包，预期优于 94.712。
2. **加合一份 `knife_convention='intended'` 的包**（配置改动），拿到 minimax 视角下的最优包。
3. **做口径判定实验**：同一天提 `merged_v2` 与（`ours_seed` 或 intended 合并包）各一发，看榜面回分。
4. 修 `merged_v2` 的覆盖率缺口（0.9994 → 1.0 值 **+0.012 分**，`repair_semi.py` 的活）。
5. 最终交付前把 `提交说明.md` 补到对方那份的详细度（数字全部由脚本落盘）。
