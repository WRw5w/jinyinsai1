# 项目协作约定

- 接手先读 README.md 与 docs/CURRENT.md；按任务查证据，不自动加载历史 ZIP、完整会话或旧每日记忆。
- 规则见 docs/RULES.md，原始约束见 data/semi/constraints.txt；不要把初赛结论外推到复赛。
- 当前校验器存在已知判据缺口；本地通过与预测高分不能当平台认证。
- 实际提交须有用户授权；操作授权按会话上下文判断，不因本文重复索要已给出的确认。
- 不把密钥、原始会话凭据、浏览器登录状态写进仓库。
- 长任务使用 task_watcher，不密集轮询；用户未指定间隔时监控心跳为 30 分钟。
- 修改现行结论时同步 docs/CURRENT.md 与对应专题；旧结论进压缩档案，不能持续追加到入口。
- 不改原始证据字节；原包哈希、平台原始回执、来源版本应可追溯。档案只按需解压到独立目录。

- 命令统一使用 aic.py；源码进 src、测试进 tests、产物进 artifacts/runs，根目录不再新增实验或提交文件。
- 路径改动须同步引用并运行 aic.py test；历史工作区按 archives/README.md 恢复。
