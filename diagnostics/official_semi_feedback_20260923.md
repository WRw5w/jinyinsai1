# 复赛 v2 官方 0 分交接报告（2026-09-23）

## 已核实的事实

- 提交文件：`submission_semi_merged_v2/复赛结果_鱼不吃猫.zip`，138587 字节，SHA-256 `852c9b8498d4205e5e69ce656b22c2389e927490132f5ce5fa367842cf1cb165`。
- 第二次表单操作在 2026-09-23 11:12 左右显示“提交成功”。重新打开表单显示新附件；从平台“下载”按钮取回的 ZIP 与上述 SHA-256 完全一致，故此次 0 分不是传错包。
- `打榜状态查询` 于 11:12:56 给出复赛记录，状态 `DONE`，总分 **0**。详情报“不可行，总分=0；违规次数：7030 条，累计扣分 35150 分；跨轮接续不连续(7030条)”，示例从“方案0批0、方案0批1、方案0批2……”开始。
- 本地旧报告 `submission_semi_merged_v2/validation_report.json` 的 94.71154834617565 是**失效的预测分**。旧 `platform_check.py` / `platform_score.py` 都只检验同一订单的轮次编号相邻，因此报 0 违规。
- 第一次表单点击（10:51）虽出现成功提示，回读仍为旧附件、打榜状态无新记录，属于未核实的提交；第二次才证实平台保存了目标 ZIP。额度按两次点击保守记为 2/5。

## 7030 的精确复现

读取**实际提交 ZIP 内的 JSON**，遍历每个方案的相邻两轮。如果两轮存在共同订单，但上一轮字典的末尾订单与下一轮字典的开头订单不同，则计 1 处。该条件计 **7030**，与官方总数及开头的“方案0批0”等位置一致。相邻两轮有共同订单的边界共 7034 处，其中 4 处满足末尾等于开头，其余正好 7030。

```python
from platform_check import read_plan
plan = read_plan('submission_semi_merged_v2/复赛结果_鱼不吃猫.zip')
bad = [(i, j) for i, batch in enumerate(plan)
       for j, (left, right) in enumerate(zip(batch['length_scheme'],
                                              batch['length_scheme'][1:]))
       if left.keys() & right.keys()
       and next(reversed(left)) != next(iter(right))]
assert len(bad) == 7030
assert bad[:5] == [(0, 0), (0, 1), (0, 2), (0, 3), (0, 4)]
```

这强烈支持平台把“中间不得插入其他订单”解释为**跨轮交界直接接续**，不能只看轮号相邻。尚未拿到平台源码；精确判定式仍是依据这次反馈反推的假设，建议用其他已知官方结果交叉验证。特别要确认“共同订单多于一个”“两轮无共同订单”“同订单跳轮”这三类边界。

## 给求解器 agent 的修改范围

1. 在 `platform_check.py` 与 `platform_score.py` 的复赛连续性规则中加入跨轮交界检查，并保留原有跳轮检查；用此 ZIP 验证连续性违规计数为 **7030**。初赛规则不能受影响。
2. 在 `solver.py` 的 `validate_plan`、方案生成/修复、跨块合并路径中维持同一规则。只修最终校验不足以得到可提交的高分解；生成过程必须能构造合法的轮次边界。
3. 为最小正反例和本包回归加测试。重建新 ZIP 后，对**实际压缩包**运行独立校验，确认 0 违规，再考虑提交。不要重复提交此 v2 包。

自动打榜代码由另一个工作流负责；本报告只交接赛题约束与求解器问题。
